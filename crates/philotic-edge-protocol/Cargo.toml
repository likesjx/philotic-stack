[package]
name = "philotic-edge-protocol"
version = "0.1.0"
edition = "2024"
description = "Wire types for the edge-mesh client-server protocol (edge clients <-> hotel termination)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Clippy ratchet: this crate is warning-free and must stay that way.
[lints]
workspace = true
