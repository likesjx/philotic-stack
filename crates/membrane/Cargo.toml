[package]
name = "membrane"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
membrane-telegram = { path = "../membrane-telegram" }
tokio = { version = "1.42", features = ["rt-multi-thread", "macros"] }
