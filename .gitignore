/target/
**/target/

# Runtime state and local databases
*.db
*.db-*
*.sqlite
*.sqlite-*
blobs/

# Local runtime configuration and secrets
.env
.env.local
.env.*.local
mesh-config.*
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets*.json

# Logs and local process output
*.log
logs/

# Python cache / virtualenvs
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS noise
.DS_Store
._*
.idea/
.vscode/
.zed/
*.code-workspace

# Misc local build results
result
ansible/vault/

# graphify knowledge graph output (rebuildable via 'graphify update .')
graphify-out/
dist-ci/

# Windsurf harness projections (generated by `phil graph harness apply`)
.windsurf/

# TLS material — a .crt landed untracked in the repo root (tailscale cert).
# The .key was already covered; the cert was not.
*.crt
