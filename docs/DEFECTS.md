---
doc_type: defect-tracker
status: active
last_updated: 2026-09-15
---

# Defects and Technical Debt

Tracked defects and known technical debt. Each entry carries status, severity, points estimate, and PR/commit cross-references. Rebuilt 2026-07-03 from session history (2026-04 → 2026-07): the ledger had not been updated since 2026-03-31 while ~30 real defects were found and fixed in the field.

**Status values**: `open` | `fixed`
**Severity values**: `high` | `medium` | `low`

## Ledger

| ID | Title | Severity | Status | Pts | Found | Fixed by |
|---|---|---|---|---|---|---|
| DEF-196 | Restart storms hit the execution transport's 64-connection inbound cap (DEF-181): after each peer restart on 2026-09-19 the vps logged 4-56 `at its 64-connection cap; refusing` lines per minute for ~2 min (95 total, all from mac-jane's queued backlog; none once settled, 0 established afterwards). Nothing is lost — the sender keeps the event and backs off — but a burst flush is refused instead of queued, and one event per connection makes the burst wide | low | open | 2 | 2026-09-19 | — (fix: queue on a semaphore with a short wait instead of refusing; make the cap per peer; and/or send a backlog over one connection — batch per frame is already step B) |
| DEF-195 | All 438 sessions on vps-jane are `active` — sessions are never closed or aged out, so every per-session sweep (silence scan, list_sessions) scales with all sessions ever opened, and `session_event` holds ~510 MB (avg 24 KB per event) with 7-day retention. Per-event payloads are far larger than the ledger needs | medium | open | 5 | 2026-09-19 | — (fix: close/age sessions with no turn in N days; cap or reference large `payload_json`; sweep only sessions with a recent turn) |
| DEF-194 | `memory.report` (`count_recent_turn_events_by_name` / `count_recent_session_events_by_kind`) filters and orders `session_event` on `$.kind` and `$.created_at`, which have NO expression index: each call JSON-parses all ~21.5k `session_event` rows (~510 MB, avg 24 KB each) — 4.7 s standalone on the vps, ~10 s under the shared graph lock, freezing the whole hotel for any agent that calls the tool. On-demand, so rare, but the same class as DEF-191 | medium | open | 3 | 2026-09-19 | — (fix: index `(kind, $.kind, $.created_at)` and tally in SQL instead of shipping 5,000 x 24 KB rows to Rust; or cache the report for 60 s) |
| DEF-193 | Avoidable steady-state mesh chatter, measured 2026-09-19: (a) `model-catalog-sync` is ~826 KB (~1.1 MB on the wire) and is pushed to each peer after every peer restart and every 6 h (~11 MB/day, multiplying in deploy waves); (b) heartbeats are 1.3 KB every 3 s per directed pair (37.5 MB/day) and hotel-state 4.7-7.9 KB every 30 s (13-23 MB/day per pair) — ~340 MB/day mesh-wide at full duty; (c) mbp and mac reconnect to vps:1235 (y-websocket) every ~3.5 s with no listener and no backoff | low | open | 2 | 2026-09-19 | — (fix: content-hash the catalog and send on change; stretch the heartbeat once TTLs allow; back off or disable the 1235 dial when nothing answers) |
| DEF-192 | mac-jane's `HotelStateSync` is 7,873 B on the wire against the 9,000 B budget (87%) and the 9,216 B macOS UDP datagram limit. The roster rides every chunk and grows with guests; past the budget the code only warns and ships anyway, macOS then fails the send (EMSGSIZE) and every peer loses mac-jane's roster — cross-hotel routing to its agents and placement gossip stop within 90 s. mbp is at 7,361 B | high | open | 3 | 2026-09-19 | — (fix: delta or paged roster / hash-then-pull; drop inactive guests from the gossip; alarm at 75% of the budget) |
| DEF-191 | The hotel process froze ~10 s out of every 30 s on vps-jane (8-13 s log silences, ~32% of wall time; every task that touches the graph blocked, so heartbeats, hotel-state, IPC and ACKs all queued and then burst — which is what produced the 1.72x duplicate-ack ratio and the 5-10 s delivery tail). ROOT CAUSE (CONFIRMED 2026-09-19 by a DWARF `perf` profile of the pegged worker + timing the queries on the vps DB): the heal-dispatcher's 30 s `RepairStaleSessionTurns` runs `scan_silent_sessions`, which calls `GraphDomain::list_session_turns` ONCE PER ACTIVE SESSION, and `list_session_turns` read and JSON-parsed EVERY `session_turn` row in the hotel then filtered by key prefix in memory. vps has 438 active sessions x ~24 ms per full read ~= 10.5 s, all under the single `std::sync::Mutex` on the graph connection, which parks every runtime worker that touches the graph. It was never the beacon receive loop, the nonce store or fsync (fsync: 248 syncs/40 s, max 18 ms). | high | fixed (deployed + verified live 2026-09-19) | 3 | 2026-09-19 | codex/def-191-session-turn-scan: `list_session_turns` now asks the `session_id` expression index for just that session's newest `limit` turns (`list_nodes_by_kind_json_eq`), oldest-first; exact `session_id` match (the old prefix filter also pulled in a longer session id's turns). Tests: behaviour + an `EXPLAIN QUERY PLAN` guard that fails if the lookup stops using `idx_graph_nodes_kind_session_id`. New `slow graph query held the shared connection lock` warning (>= 250 ms) on the two scan methods so the next stall names itself. Also from the earlier slices: #571 (registry lock no longer held across SQLite writes, in-memory nonce window) and #572 (ACK grace + per-peer backoff). Re-measure gap % and the duplicate-ack ratio after deploy. LIVE RESULT (develop 7a05b069, all 3 hotels, vps restarted 20:42:54 UTC): 0 log silences >= 5 s in 337 s on the vps (was ~32% of wall time, 8-13 s every ~30 s); mac 0%; cross-hotel round trip mac -> Beacon(vps) -> reply 2.3 s. Duplicate-ack ratio not re-measured yet (expected to fall toward 1x now that the receiver no longer stalls). |
| DEF-190 | Mesh observability: an operator cannot answer "did this task reach hotel B, when, and what did B do with it?". `corr_id` is a constant literal on the commonest path (`"emit"` for every EmitTask); `EventEnvelope.trace` is never populated except on cron envelopes that `AppendLocal` drops; `created_at` is 0 and `source_agent_id` `"unknown"` on EmitTask; there are no counters; inbound events are appended then immediately deleted so the receiver keeps no record; the only view (`hotel_status.mesh_peers`) reads an unfiltered roster. Found by the 2026-09-19 mesh protocol review | medium | open | 5 | 2026-09-19 | — (fix: real ULID `corr_id` on every envelope logged at each hop, a 3-point `trace`, a 24 h `mesh_event_log` + `phil mesh` read surface) |
| DEF-189 | A stale, unmarked local `command_manifest` apartment (left from when the agent last ran on this hotel — mbp-jane holds a 15-entry one for Beacon) is treated as "locally hosted": it refuses the hosting hotel's push (DEF-180) and is re-broadcast every 120 s for peers to refuse. Found live 2026-09-19 | low | partial (fix deployed; send-on-change open) | 1 | 2026-09-19 | PARTIAL, codex/def-189-manifest-hosting: "hosts" is now ONE definition both ways — an ACTIVE local guest of the agent (`agent-x` or `agent-x:role`). A hotel re-broadcasts only agents it runs, and a leftover unmarked apartment with no active guest is superseded by the host the roster names instead of refusing it forever. The steady-state refusals (271 WARNs in 18 min on the vps after the d624e5b8 deploy) are logged loudly once per (peer, agent, reason) then at debug. STILL OPEN: every hotel re-sends all its agents' manifests (~4 KB each) to every peer every 120 s regardless of change — send on change, then a slow heartbeat (~15 min) or a digest LIVE: deployed 7a05b069; vps manifest-refusal WARNs fell from 271 in 18 min to 6 in 6 min (the once-per-peer/agent/reason first sightings; mbp was still on the old build for part of that window). |
| DEF-188 | Mesh freshness/convergence gaps: `NodeRegistry::remote_hotel_states()` has no freshness filter (`hotel_status` shows a dead peer's roster as current); capability-sync advertisements ride a node entry refreshed by the 3 s heartbeat so they never expire even if the advertiser died; one lost capability-sync chunk leaves a peer stale for up to 3600 s (the fingerprint is recorded after sending; a restarted peer lacks advertisements for up to an hour); `HotelStateSync` is a pure 30 s timer although the code comments say it fires on change, so a placement change (relocation SWITCH) reaches peers up to 30 s late (60 s after a lost datagram) | medium | open | 3 | 2026-09-19 | — (fix: filter the roster view, expire advertisements independently, sync version/digest in the heartbeat + resend on reconnect, change-driven roster with a 1-2 s debounce) |
| DEF-187 | Mesh authentication covers too little: the HMAC signs only `msg_id, seq, timestamp, payload` — not `msg_type`, `src_node`, `dest_node`, `version` or `total` — with one symmetric key per pair used in both directions, so an on-path attacker can reflect a node's own frame back relabelled as its peer's and make the peer advance and delete the victim's cursor; `dest_node` is never checked on either plane; no key id/epoch so no rotation without a flag day; `BeaconMessage.seq` is u32 while the HMAC signs the u64 event seq (every packet fails once a ledger passes 2^32). Extends DEF-175 | medium | open | 5 | 2026-09-19 | — (fix: per-direction HKDF subkeys + MAC over the whole header with dual-accept rollout, a `key_id`, widen `seq`; consider Noise on the execution plane) |
| DEF-186 | Streaming chatter crosses the mesh. The model router emits a `streaming_token` per SSE delta and philote re-emits a CUMULATIVE `partial_reply` per token (O(n^2) bytes) with no coalescing; when the Telegram seat is on another hotel every one crosses (persisted, sent, acked, deleted); the only throttle (`STREAMING_THROTTLE_MS` 1500) is downstream at the seat. About 20-115 cross-hotel events per turn, each ~7 SQLite writes and 2 TCP connections; final replies queue FIFO behind stream chatter | medium | open | 3 | 2026-09-19 | — (fix: rate-limit / supersede `partial_reply` per (session, turn) when the target is remote, send deltas, give final replies and tool results priority) |
| DEF-185 | Mesh hot-path inefficiency (MEASURED live 2026-09-19: the replay-nonce stores never shrink — sidecar `nonces.db` 2.73M rows / 274 MB on mac, 4.09M / 411 MB on vps, 4.01M / 404 MB on mbp, oldest rows from May, growing ~6 MB/day per hotel, plus `mesh_nonces` in the live context DB at 826k rows on mac; the per-message insert into the live hotel DB coincides with the observed `database is locked` / `unable to open database file` packet drops): the dispatcher only polls (1 s tick, no wake on append) so every hop adds 0-1 s (mean ~500 ms); ONE event per TCP connection and one more connection per ACK; payload amplification 2.2x at 1 KB / ~1.5x at 20-200 KB (JSON-escaped string in JSON, base64, JSON again; no compression; `BlobRef` never emitted; a second serialization only to log bytes); the receiver opens a NEW SQLite connection per message for the nonce check and `clean_expired_nonces` has zero callers so `mesh_nonces` grows forever; the receiver INSERTs every inbound event then DELETEs it; ~7 write transactions per event; `mesh_events` has no index on `target_node_id` so each peer's poll walks the whole backlog | high | open | 5 | 2026-09-19 | PARTIAL, codex/mesh-nonce-window: the replay window is now an in-memory map (5-min window, swept every 30 s, capped at 500k, a shared process-wide tracker for the execution plane), a packet stamped before this process started is refused so a restart is not a replay window, and the legacy `nonces.db` sidecars are deleted and the `mesh_nonces` table dropped at startup. Still open: (fix in order: `Notify` wake on append, batch events per frame, shared/in-memory nonce window + pruning timer, skip the INSERT-then-DELETE, index `(target_node_id, seq)`, then compression / raw-bytes framing / persistent connection with in-band ACK) |
| DEF-184 | Every mesh envelope with `target_node_id: None` is silently dropped by `LedgerCommand::AppendLocal` (`is_local` defaults to true when there is no target; the DEF-139 warning fires only when `target_agent_id` is set). All three cron control-plane emitters (`CronFired`, `CronJobSync`, plus remove) build such envelopes, so mesh-coordinated cron never leaves its hotel and the receive handlers have never been reachable; `AgentGraphSync` has no emitter at all. (`command_manifest` sync avoids this by addressing each peer.) Verified in `main.rs` | high | open | 2 | 2026-09-19 | MITIGATED, codex/mesh-sender-binding-udp: the drop is now logged once per event kind (it was silent unless `target_agent_id` was set). DECISION NEEDED before fixing it: making a no-target envelope fan out to every peer switches on `CronJobSync` replication and `CronFired` suppression, which have never run on the wire — replicated jobs could fire from several hotels and send the operator duplicate daily briefs (the DEF-166 shape). Options: (a) leave cron hotel-local and delete the dead broadcast emitters, (b) enable fan-out plus an explicit per-job `replicate` opt-in. Command-manifest sync avoids the problem by addressing each peer |
| DEF-183 | The DEF-170 sender binding covers only the event plane. On the UDP gossip plane `Heartbeat`, `CapabilitySync`, `HotelStateSync` and `MeshCatalogSync` trust node ids INSIDE the payload and never compare them with the authenticated `msg.src_node`: any enrolled peer can make every hotel believe it hosts an agent (roster injection), rewrite another hotel's `mesh_port`/host/execution port, or feed placement gossip for another node. Also on the event plane: the control-plane `match` (`CronJobSync`, `ProjectedUserIdentitySync`, `session.handoff`) runs for events addressed to a THIRD node with no target check, and `session.handoff` (`handle_remote_role_handoff`) is not even given the source node and upserts a caller-supplied role record + spawns a guest with no authority check | high | fixed | 3 | 2026-09-19 | codex/mesh-sender-binding-udp — `Heartbeat`, `CapabilitySync` and `HotelStateSync` are dropped unless the node id they describe equals the authenticated `msg.src_node` (so a forged heartbeat can no longer create a registry entry or rewrite another hotel's stored port, and a forged roster no longer injects agents); the control-plane handlers no longer run for events addressed to a THIRD node; `session.handoff` now goes through `admit_handoff_role_record`: an existing local role record is never overwritten (the old code upserted unconditionally although its doc said "if not already present"), a record for an agent with a known authority hotel is accepted only from that hotel, and a peer never grants `is_admin`. Not changed: `MeshCatalogSync` is by design a peer relaying OTHER peers' ports (hints; only `mesh_port` and an empty `mesh_host` change) |
| DEF-182 | Mesh delivery can silently LOSE events and is fragile across versions. The inbound loop is `if let Ok(events) = from_slice::<Vec<EventEnvelope>>(..)` with no `else`, and `EventKind`/`EventPayload` have no tolerant variant, so an unknown kind from a newer hotel is dropped without a log; because acks are cumulative (`MAX` cursor) and the sender deletes every row `seq <= acked`, one dropped event followed by a parsed one is deleted from the sender's ledger for good, and if ALL pending events fail to parse the sender re-sends them every second forever (each attempt writing a new nonce row). The ack is sent unconditionally — even when delivery dropped the task ("no subscriber … task dropped") — and destroys the only durable copy; `attempt` and `expires_at` are dead fields (no `attempt++`, no expiry predicate, seconds-vs-ms mismatch), there is no dead-letter path or per-target cap, and inbound events for other nodes / failing the sender check are still appended to the receiver's ledger (NULL-target rows are never deleted). Mixed-version windows are exactly when relocation ceremonies run. Verified in `mesh_runtime.rs` | high | open | 5 | 2026-09-19 | — (fix: tolerant `EventKind`/`EventPayload` + per-event parse; ack only what was delivered or parked and dead-letter the rest; `attempt++`, enforce `expires_at`, per-kind TTL for chatter; heartbeat `protocol_version` + `features` gating new kinds) |
| DEF-181 | One unreachable mesh peer stalls cross-hotel traffic to every other peer, and the inbound ACK path can freeze ALL inbound mesh processing. `send_execution_message` has no connect, read or write timeout (a black-holed peer waits the OS SYN timeout, ~75 s macOS / ~127 s Linux); the dispatcher walks peers sequentially, so one bad peer delays the rest every tick; the ACK connect is awaited INSIDE the single inbound loop, so heartbeats, other peers' batches and WebRTC signals stop for the same window; a slow Muninn write in delivery does the same. Also: `read_execution_message` allocates the peer-claimed u32 length before authenticating (DEF-175) with no read timeout or connection cap, and an `accept()` error permanently ends the execution plane until restart. Verified in `execution_transport.rs` / `mesh_dispatcher.rs` | high | fixed | 3 | 2026-09-19 | codex/mesh-timeouts — connect (3 s) and write (5 s + 1 s/MB) timeouts; every peer is dispatched to in its own task with an in-flight guard and exponential backoff (1 s → 30 s) so a dead peer cannot delay the others; the ACK is sent from a spawned task, not inside the single inbound loop; inbound frames are capped at 32 MiB with the length checked BEFORE allocating, memory growing only as bytes arrive, a 30 s read timeout and a 64-connection cap; an `accept` error is logged and the loop continues. Still inline in the inbound loop (tracked under DEF-182/191): the delivery `await`, which a slow Muninn write can hold |
| DEF-180 | A Telegram seat polling on a different hotel from its agent registered only the 4 built-in commands, REPLACING the bot's real menu (24 commands for Beacon: 4 native + 21 agent), and `/help` omitted the agent's commands. The seat reads the agent's command manifest from its OWN hotel's graph (`__apartment__:{agent}:command_manifest`), which the philote writes only on the hotel that runs it. Live 2026-09-19 during the cross-hotel test: mac-jane's Beacon seat logged `Agent command manifest not yet available; using native commands only` and `Registered 4 Telegram bot commands (4 native, 0 agent)`; the vps re-registered 24 when the bot moved back. Commands themselves always worked — the seat forwards any command it doesn't handle to the agent wherever it runs | medium | fixed | 3 | 2026-09-19 | codex/telegram-commands-cross-hotel — the hotel that hosts an agent pushes its manifest to every peer when the philote publishes it and re-broadcasts every 120 s (first pass after 45 s) so a restarted or new peer catches up; peers cache it as the same apartment, so the seat's existing local read just works. Carried as a `SessionControl` event with its own action (older hotels ignore it). A peer only accepts a manifest from the hotel its roster says runs the agent, never overwrites its own agent's manifest, never re-broadcasts a peer's copy, and caps a manifest at 200 entries |
| DEF-179 | A task forwarded across hotels for a BASE agent id was delivered to the wrong process on the hosting hotel. `deliver_event_envelope_or_park` trusted the `delivery_target_guest_id` the SENDING hotel stamped — for a Telegram seat that is the base agent (`agent-beacon`), the only thing it knows. On the vps that delivered the turn to the base process while the model's answer was routed to `agent-beacon:orchestrator` (the process that really owns Beacon's sessions), which had no active turn: `handle_model_response: no active turn for session — dropping response`. It also overwrote the session's `active_incarnation_id` with the base id. Found by the live cross-hotel smoke for DEF-178 (synthetic seat on mac-jane -> Beacon on the vps: the turn ran, the reply never came back) | high | fixed | 2 | 2026-09-19 | codex/cross-hotel-agent-process — the hosting hotel resolves the process itself for a task addressed to a base agent id: the session's own routing first (`resolve_agent_route`), then the agent's live `:orchestrator` incarnation; a sender that named an incarnation (handoffs, paracrine replies) is delivered as addressed, and a single-process philote under the base id keeps getting it |
| DEF-178 | A Telegram seat could not reach an agent that runs on another hotel, so a transport could only ever poll on the hotel that hosts its agent (Beacon's bot polled from mac-jane was answered by Bjork, DEF-177). Two causes: (1) roster lookups (`find_node_id_for_guest`) expired a peer's gossiped roster after the 15 s node TTL although rosters are re-broadcast every 30 s, so cross-hotel routing by roster failed for 15 s of every 30 s; (2) nothing resolved a BASE agent id (`agent-beacon`, what the seat addresses) to the hotel that runs it — rosters list role guests (`agent-beacon:orchestrator`) — and `peer_agent_node_from_roster` took the first hotel listing the agent's identity, which can be a standby that only replicates it (mbp-jane lists Beacon) | high | fixed | 3 | 2026-09-19 | codex/telegram-cross-hotel-routing — roster freshness is its own 90 s window (three missed broadcasts); `find_node_id_for_agent` / `best_host_for_agent` rank hotels by an active guest of the agent, then any guest, then the identity alone, ties broken by node id; `resolve_guest_home_node` falls back to it, so a seat's task for an agent the hotel doesn't host is forwarded over the mesh and the reply returns to the seat's own hotel. Limit: the seat can't read the agent's command menu across hotels and shows only its built-in commands |
| DEF-177 | A task addressed to an agent a hotel does not host was answered by ANOTHER agent's orchestrator. Live 2026-09-18/19: Beacon's Telegram home was moved to mac-jane (which has no Beacon identity); after the DEF-169 fix mac-jane's Beacon seat could take the lease, and every message to Beacon's bot was delivered to `agent-bjork-01:orchestrator` and answered as Björk through Beacon's bot. Two leaks: `resolve_orchestrator_guest_id` ended with "any live orchestrator on the hotel" even when the session names its own agent, and `resolve_agent_route` returned `Deliver(None)` for an unresolved named agent, dropping the address so a role-wide agent delivery reached whoever was subscribed. The session row on mac-jane was also created with Björk as its primary agent | high | fixed | 3 | 2026-09-19 | codex/no-cross-agent-fallback — a session that names a primary agent only falls back to THAT agent's orchestrator (no primary agent keeps the hotel-orchestrator fallback); an unresolved named agent keeps its address (delivered to it or not at all, never to someone else); the EmitTask last-resort fallback refuses a cross-agent orchestrator loudly |
| DEF-176 | Telegram bot tokens were plaintext `config:` values (seeded by `aiua load` from `mesh-config.json`), readable by any local process through ACL-less `GetConfig`, and outside the vault — so no relocation could ever carry one sealed; the migration bundle treated the value as a vault ref and exported nothing | high | fixed | 3 | 2026-09-18 | codex/telegram-tokens-in-vault — after every load and at every boot, a plaintext `telegram_bot_token[_{agent}]` value moves into the vault (kind `telegram_bot_token`, readable by the `membrane` role only) and the key keeps its name with the vault ref as its value; `aiua load` rotates a vaulted token in place instead of overwriting the ref; the Telegram seat resolves a `secret://` value through `GetSecret` (lease fingerprints are unchanged — they hash the resolved token) |
| DEF-170 | Any authenticated mesh peer could speak as any other hotel. The batch HMAC proves who sent a batch, but handlers acted on `event.source_node_id` from inside the payload — so a peer could send a `MaterializeRequest` (planting an admin role record and spawning a guest), a `ContinuityImport` (overwriting another agent's checkpoints), or a reply as another hotel. Nothing checked the sender was the role's home either. Found mapping R7 sealed-secret transfer | high | fixed | 3 | 2026-09-18 | codex/mesh-sender-binding — events addressed to this hotel are dropped unless `source_node_id` equals the HMAC-authenticated `msg.src_node`; `MaterializeRequest` and `ContinuityImport` are refused unless the sender is the role's known home (or the role is unknown here); a continuity bundle must name its sender as origin |
| DEF-171 | Placement gossip (`placement_sync`, role homes + transport homes over `HotelStateSync`) is last-writer-wins by a sender-supplied timestamp from any peer: a peer can claim any role's or transport's home with `updated_unix=u64::MAX`. DEF-170's role-home check reads this record, so it raises the bar but does not close the path. Placement records must never authorize a secret release | high | open | 3 | 2026-09-18 | partial (codex/mesh-sender-binding): a stamp more than 300 s in the future is refused, so a home can no longer be pinned forever. Open: who may move a home — a peer can still claim one with a current stamp (the real home re-claims it). A plain "only the current home may publish" rule would also block the agent's own orchestrator setting its transport home from another hotel |
| DEF-172 | Operator-surface handoffs that change state (`secrets.add`/`secrets.rotate` of ANY secret ref including mesh private keys, `config.set`, `components.register/set_active/restart/remove`, `roles.set_home`, `agent.deploy_bundle`) were applied from any mesh peer; `agent.deploy_bundle` fetched an arbitrary URL and applied a bundle of plaintext secrets (empty role ACL), config writes and guest spawn commands. The agent migration bundle exported the Telegram token in plaintext to the unauthenticated blob store | high | fixed | 3 | 2026-09-18 | codex/mesh-sender-binding — read-only surfaces stay open to every peer; state-changing ones apply only from the hotel itself or a node in `operator_authority_nodes` (deny by default); `agent.deploy_bundle` is retired from the mesh; the migration bundle never exports vault entries |
| DEF-173 | Local IPC identity is self-asserted: any same-user process can `Register` as `hotel.internal` and read every vault secret (incl. mesh private keys) via `GetSecret`, read/write any config via ACL-less `GetConfig`/`SetConfig` (incl. `mesh_auth_key:*`), call `RotateSecret`, or `ApplyAgentBundle` (arbitrary guest spawn). The same user can already read `context.db` and the vault master key file, so the Unix user is the real boundary today | medium | open | 5 | 2026-09-18 | — (fix: peer-credential check on the socket + per-guest capability tokens; socket-mode hardening; drop the `/tmp` socket fallback) |
| DEF-174 | Mesh join: the inviter pins the joiner's Ed25519 key by `hotel_name` but stores `mesh_auth_key` by the joiner-claimed `node_id`, so an invite holder can join under a new hotel name while claiming an existing peer's `node_id` and overwrite that peer's auth key. The joiner never pins the inviter's Ed25519 key | medium | fixed | 2 | 2026-09-18 | codex/sealed-secret-transfer — a join is refused when its claimed node id already belongs to a different hotel (prerequisite for R7: a sealed secret goes to whoever holds the pair key) |
| DEF-175 | Execution plane: `read_execution_message` allocates whatever `u32` length a peer claims before authenticating (pre-auth memory DoS; the vps listener binds `0.0.0.0`); the batch HMAC covers `msg_id, seq, payload, ts` but not `msg_type`/`src_node`/`dest_node`; vault AES-GCM has no AAD binding ciphertext to its `secret_ref`; docs still describe a `PHILOTIC_MESH_PSK` fallback no code reads | medium | open | 3 | 2026-09-18 | — |
| DEF-169 | `handle_acquire_telegram_poll_lease`/`handle_renew_telegram_poll_lease` both required an `agent_identity` lookup unconditionally, up front, even though it is only used inside the `Ok(None)` legacy-authority fallback branch of the transport-home check that follows. A hotel with an explicit, authoritative `MembraneTransportHomeRecord` (which needs no agent-identity lookup at all) was still refused with `LEASE_AGENT_UNKNOWN` whenever that agent's identity had never been seeded on this hotel — exactly the case for any agent whose seat was discovered dynamically via R2/DEF-157 rather than listed in the hotel's static mesh-config roster, since agent identities are seeded from that same static roster. Live on mac-jane: after DEF-143/DEF-157/DEF-168 all verified working (transport home correctly flipped, gossip correctly computed `hotel_is_home=true`, the resulting denial correctly and quietly classified as Standby by DEF-168's fix), the Beacon seat still never acquired the lease — every attempt hit this identity gate first, because mac-jane has no `agent_identity` node for `agent-beacon` at all. Found immediately after DEF-168 deployed, mid-R2 watched-live UAT re-run | high | fixed | 2 | 2026-09-18 | codex/def169-lease-identity-gate — `agent_identity` is fetched lazily, only inside the `Ok(None)` branch that actually uses it, so an explicit transport-home verdict settles the question on its own |
| DEF-168 | A Telegram poll-lease denial with no competing owner (`owner: None`) was only classified as a quiet Standby reprobe when the hotel's refusal code was exactly `LEASE_TRANSPORT_HOME_MISMATCH`; every other ownerless denial (chiefly `LEASE_FOREIGN_AUTHORITY`, which a dynamically-discovered R2 seat hits on a hotel never in the agent's static authority list) fell through to the noisy `LeaseHeld` branch — the seat logged "held by None", stood down convinced a competitor held the lease, self-healed on the next 180s reprobe finding it free, and immediately re-denied itself the same way, looping forever without ever acquiring even while genuinely the transport home. Live on mac-jane: Beacon's dynamically-discovered seat cycled `seat_stood_down:lease_held`/`seat_lease_self_healed` every ~200s continuously since boot (2026-09-17 15:07 UTC onward), 190+ occurrences, zero successful acquires — found mid-R2 watched-live UAT when the operator's test message would have had nothing listening on the mac-jane side | high | fixed | 2 | 2026-09-18 | codex/def158-transport-home-denial — `owner.is_none()` alone (not a specific denial code) is now the Standby/LeaseHeld discriminator in `TelegramSeatGuest::setup`'s acquire loop; the denial code is still logged for diagnostics |
| DEF-167 | A philote restoring a session got defaults for most of its checkpoint — even on the same hotel. `ensure_session_loaded` reads the hotel's composed `__session_snapshot__`, which projected only `recent_turns`/`active_turn` out of the apartment, so a parked plan/approval turn, the carryover plan, paracrine threads, watchdog clocks, the fallback override and the life-recall cache were dropped on every restart. Separately, `list_apartments` took the last `:` segment as the memory type, so every `short_session:{sid}` checkpoint listed as the agent id: the startup stale-turn sweep never ran and the agent migration bundle carried zero session checkpoints. Found while designing relocation continuity (R5) | medium | fixed | 3 | 2026-09-18 | codex/relocation-continuity — the snapshot overlays every checkpoint key it does not compute itself (hotel-owned routing/status/policy keys win); `list_apartments` returns the whole memory type; the startup sweep reads the raw checkpoint (never the lossy snapshot it would write back) and only sweeps its own process's keys |
| DEF-166 | Every daily Bjork cron brief was also sent by the Coach bot. A reply that names no seat reaches every Telegram seat on the hotel, and a seat decided ownership from the session id; `cron:<job_id>` sessions (e.g. `cron:lifegraph-flywheel-daily:mac-jane`) name no agent, so each seat accepted the reply. Live 2026-09-13..18 on mac-jane: the 08:00 flywheel brief and the 09:00 architect-charter brief went out as Bjork and Coach | medium | fixed | 2 | 2026-09-18 | codex/cron-session-owner — aiua `EmitTask` stamps `reply_owner_agent_id` from the emitting agent's registered identity on every seat-less membrane task; the Telegram seat drops tasks owned by another agent and keeps the session-id parse as fallback |
| DEF-161 | Regression from DEF-158: the write-time duplicate guard applied to every label, including system telemetry. Attention Signals legitimately repeat identical text under fresh ids (the live graph held 286 Signals over 13 distinct summaries), and the philote's attention observer writes them fire-and-forget via `life.observe`, so from the DEF-158 deploy (2026-09-17 21:31 UTC) a repeated signal was refused and dropped silently. Bridging would likewise have linked telemetry to lived nodes | high | fixed | 1 | 2026-09-18 | `codex/guard-skip-telemetry`: `hygiene::guard_applies_to_label` — the guard and bridging apply only to lived-fact labels; `audit::SYSTEM_LABELS` (Signal, patches, DriftFinding, Growth*, ConflictHandoff, OntologyExtension) are exempt. Found while answering the operator's "is this global? is it tool-based?" |
| DEF-160 | Nothing connected an incoming claim to what it names, so the graph grew as a hub with spokes: the gardener's only remedy for an island was `SCOPED_TO` the operator's chief-of-staff role, and every domain landed there. Live 2026-09-17: "Jared dropped Daxton off for his class" was written with no edge to `life:person:daxton` until the operator asked for the link by hand ("we need to make sure we link Daxten's Dropbox to Daxen as a person… I don't know how you do that"); a practice Event naming Moonlight III had no edge to the CreativeWork; organ/piano work anchored to chief-of-staff while `life:role:organist` and `life:role:pianist` sat unused | high | fixed | 3 | 2026-09-17 | `codex/lifegraph-bridging` (LifeGraph pristine, layer 3): new `bridging` module — `bridges_for` returns the edges the ontology allows between a claim and the live nodes it names (distinctive tokens only: generic words like "practice"/"session" never bridge), `validated_rel_type` reads the pair from `AGENDA_EDGE_RULES`, `category_roles_for` finds the Role a claim belongs to. `life.observe` writes the endpoint-validated edges itself (Event INVOLVES Person, OpenLoop ABOUT Subscription, Routine OCCURS_AT Place — stamped `bridged_at`/`bridge_match`) and reports the rest as `suggested_edges`; the audit anchors an orphan to the category Role it names, falling back to the anchor role |
| DEF-159 | The sweep could describe the graph's contradictions but never close them: a live loop and a completion record for the same work sat side by side forever (live 2026-09-17: `life:open_loop:pay_bills_20260916` confirmed and open, `life:open_loop:pay_bills_event_20260916` an Event marked done, plus a third bills Commitment written that morning), past-dated Events stayed live against the operator's own standing rule, and a duplicate whose loser was CONFIRMED was skipped silently — which is how one evening of practice kept five confirmed Events | high | fixed | 3 | 2026-09-17 | `codex/observe-duplicate-guard` (LifeGraph pristine, layer 2): `AuditReport.closable` collects `contradicted_by_completion` (a live loop-like node whose claim a completion record covers — matched by summary overlap OR `hygiene::id_slug_overlap`, which sees "pay_bills" through "needs to pay" vs "paid") and `past_dated` (a live dated record whose date has passed), each emitted as a `life.tidy resolve` the sweep applies itself; a confirmed twin is no longer skipped but named in `needs_judgment` with what the operator must decide |
| DEF-163 | A say-do re-entry discards the refused reply, so anything the model DERIVED in it is lost — above all an attachment's contents, which exist in text only there. Live 2026-09-17 09:09 EDT: the operator sent the Sunday hymn program as a photo with the caption "Here are the updated hymns… 1069 is the choir"; the turn routed to `analyze_media` (tool-less), the vision model read the program CORRECTLY (#26, #136, #1069 choir, #85) and replied "I have updated the hymn list", the gate refused that as an unbacked claim, and the text-only retry (0 attachments, reading gone) rebuilt LAST WEEK's list from `memory.recall` (#242, #219) and committed it as "updated". The operator found out two turns later | high | fixed | 2 | 2026-09-17 | codex/media-evidence-carry — `reenter_for_say_do_check` takes the refused draft and prepends `[Your previous reply — NOT sent to the operator]` plus "reuse it verbatim, do not re-derive it from memory" (4 000-char cap). Applies to every re-entry (say-do, claim-over-failure, plan gate) |
| DEF-164 | An attachment sent with NO caption is answered with a description even when the operator's last request is still open. Live 2026-09-17 09:15 EDT: bjork had just asked "Could you send the new hymn numbers again?", the operator re-sent the program photo, and the media turn replied with a hymn-by-hymn essay on the screenshot while the update stayed undone | high | fixed | 2 | 2026-09-17 | codex/media-evidence-carry — `WorkingTurn.media_analysis` marks a vision/document dispatch; when the caption is the membrane placeholder ("User sent a Telegram photo.") AND `SessionState::operator_request_open` (last assistant reply asks the operator for something, or a carryover plan has steps left) the analysis re-enters the cognitive loop as INPUT (`MEDIA_EVIDENCE_REENTRY_HINT`, event `media_evidence_reentry`), tools intact, once per turn |
| DEF-165 | `life.observe.batch` items never got the identity fields a single `life.observe` synthesizes, so a model-authored batch was rejected wholesale on `packet_id must not be empty` and cost a retry model call. Live 2026-09-17 09:16 and 09:18 EDT: both of bjork's batches failed their first attempt this way (the retries landed) | medium | fixed | 1 | 2026-09-17 | codex/media-evidence-carry — `handle_observe_batch` calls `normalize_defaults()` on every item after parse, before plan/validate; supplied ids are never overwritten |
| DEF-158 | Nothing stopped the same lived fact from being written again under a new id, so the graph grew contradictory twins faster than the sweep could collapse them. Live 2026-09-16/17 (Beacon): one evening of practice became five confirmed Events; a misheard "Jackson's Drop-off Routine" stayed beside the corrected "Daxton's"; a second bills item (`life:commitment:pay_bills_20260917`) was written while the operator was saying "I already have that as an open loop", beside a confirmed OpenLoop and a confirmed Event asserting the bills were already paid. `life.audit` finds duplicates only after the fact, and refuses to retire confirmed ones without operator approval | high | fixed | 3 | 2026-09-17 | `codex/observe-duplicate-guard` (LifeGraph pristine, layer 1 of 4): `hygiene::duplicate_guard_candidates` compares the pending claim against live same-label nodes (exact normalized summary, else singular-folded token overlap); `life.observe` REFUSES a new id whose claim a live node already carries (≥ 0.80 overlap, contract error naming that id — nothing written) and attaches `duplicate_candidates` + a consolidate instruction to the write when the match is weaker (≥ 0.55). Re-observing the SAME id is the update path and is never blocked; `force_new: true` is the escape hatch, documented in the catalog. Layers still to build: audit findings for loop-vs-completion contradictions and past-dated items, a two-lane sweep (auto vs operator review), and a proposed-backlog pass |
| DEF-157 | `IpcResponse::TransportHomeChanged` is broadcast to EVERY locally-connected guest on a hotel (`ipc.rs`'s local push after `transport.set_home`, and `main.rs`'s gossip-received push), not just the membrane seat it's actually for — but `philotic-client`'s `is_push_message`/`is_ignorable_push` never classified it, so any OTHER guest's `recv_task()`/`read_matching_response()` call bailed with "Unexpected non-push IPC response" whenever the broadcast landed mid-wait on their connection. Live 2026-09-16/17 re-running the Relocation Ceremony's R2 gate (post-DEF-143 fix): guests with no interest in the transport at all — router-listener, model-router, philote-worker, egress-http-runner, membrane-discord, and membrane-telegram's own generic `MembraneRuntime` select loop — all hit this on both flip directions; router-listener logged a "fatal error" and membrane-telegram's Beacon seat went silent and needed a manual `RestartComponent` to recover, extending the outage `RestartComponent` was meant to end quickly | high | fixed | 1 | 2026-09-17 | `codex/def-157-transport-home-changed-push`: added `IpcResponse::TransportHomeChanged` to `is_push_message`, the same category as `InboundTask`/`ApartmentUpdate`/`MuninnStatus`/`NetworkState` — `recv_task()` now returns it cleanly to every caller instead of bailing; the one guest that actually acts on it (`membrane-telegram`'s `handle_push`) already matched on it explicitly, so this only stops the false-bail for everyone else |
| DEF-144 | Every `life.observe` from Beacon on 2026-09-16 (11:17–11:49 UTC, ten calls) was refused: `contract_error: failed to parse life.observe parameters … invalid type: string "{\"title\":\"Pay bills\",\"status\":\"open\"}", expected a map` — the model sends `evidence.properties` (schema: free-form object, `additionalProperties: true`) as a JSON-encoded string; it had only started populating `properties` that day. Four operator items were never recorded (pay-bills loop + done event, an Apple News CreativeWork, a Dr. Squatch loop). Same morning 12:44 UTC: "cancel Bronze David Bars" ran only `memory.recall`, then replied "I will make sure this is tracked and followed up" — the say-do gate delivers any turn with a tool call, read-only or not, and the phrase matched no promise pattern | high | fixed | 2 | 2026-09-16 | `codex/lifegraph-lenient-properties`: `EvidencePacket.properties` deserializes leniently (object, or a JSON string of one; a non-object string fails by name); the `life.observe` schema says "pass a JSON object, never a JSON-encoded string"; the say-do gate counts only a successful non-read-only call as work done (`plan_eval::tool_name_is_read_only`) and learns the "I will make sure this is tracked/recorded/logged" family |
| DEF-143 | `transport.set_home` (since DEF-124) canonicalizes `target_hotel` to the hotel's node_id before storing `membrane_transport_home.active_home_hotel`, but three comparisons never got the matching update and still compared against the bare `hotel_name`: the Telegram poll-lease acquire/renew gate (`hotel_may_poll_transport_home`, `lease_handlers.rs`), the gossip-received `TransportHomeChanged` push's `hotel_is_home` computation (`main.rs`), and the local immediate push's `hotel_is_home` computation (`ipc.rs`). Live 2026-09-15/16 running the Relocation Ceremony's R2 watched-live gate: flipping Beacon's Telegram transport home from vps-jane to mac-jane and back left BOTH hotels refusing to acquire the poll lease for their own node_id-stored `active_home_hotel` (`LEASE_TRANSPORT_HOME_MISMATCH`, "hotel [vps-jane] is not the active telegram transport home"), taking Beacon's Telegram bot offline for ~6 hours since it was the first live `transport.set_home` call since DEF-124 landed (every existing record had stayed in its original bare-name form until then) | high | fixed | 2 | 2026-09-15 | `codex/def-143-transport-home-node-id`: all three sites resolve `active_home_hotel` via `resolve_hotel_node_id` (bumped to `pub(crate)`) before comparing against the canonical `local_node_id`, so both the legacy bare-name form and the post-DEF-124 node_id form match correctly. Regression test seeds a transport home stored in node_id form and asserts the lease still acquires. Immediate live mitigation: operator patched the live record back to the bare-name form by hand while the code fix was in flight |
| DEF-145 | `[Skill guidance]` was composed inside the Identity layer under `persona_chars` (6 000): bjork's identity + soul + governance used ~5.3 KB, so the guidance of 9 of the 12 skills projected on 2026-09-15 22:54 EDT — `music.repertoire-gardener` among them, the skill her rule says to "always use" — was cut by the layer truncation with no notice from the guidance cap. DEF-142's projection was correct (the request's projection list held the gardener); the model just never saw its doctrine | high | fixed | 1 | 2026-09-16 | codex/observe-properties-coercion — `project_skill_guidance_section` renders the section on its own and `build_context_projection` budgets it under the previously reserved `skills_chars` (2 500 ≥ the 2 400 guidance cap), appended after the budgeted core identity; test: guidance survives a 9 KB persona |
| DEF-146 | Every `life.observe` carrying the R1 `evidence.properties` map failed live (2026-09-15 22:55 EDT, 2026-09-16 09:49/09:50 EDT, bjork/Gemini): the model serialized the nested map as a JSON STRING, the runner refused it (`invalid type: string …, expected a map`), the automatic retry re-sent the identical string, nothing was written — and the reply still carried `✅ Written this turn: observed …` because `distill::tool_result_is_error` did not know the philote's own `Tool call failed: …` rendering, so the receipt, the plan verifier and the claim gate all read the failure as ok | high | fixed | 2 | 2026-09-16 | codex/observe-properties-coercion (PR #530; the runner-side lenient parse landed minutes earlier in #531 under the same id, kept theirs) — `tool_args::coerce_tool_call_arguments` repairs stringified objects/arrays against the tool's catalog `input_schema` before the call is stored or dispatched (recursive; strings that do not parse to the declared shape pass through); runner-side `deserialize_properties_leniently` accepts a JSON-string map; `tool_result_is_error` recognizes `Tool call failed:` / `Capability call failed:` / `Tool execution failed:` / `Unexpected hotel response` heads. Tests from the live shapes |
| DEF-147 | A `life.observe.batch` call could never verify plan steps bound to `life.observe`, and its items were never argument-repaired: `plan_eval::step_tool_compatible` is exact-name and one call verifies at most one step, and the batch schema declares `items: {type: object}` so `tool_args` could not reach `evidence.properties`. Live 2026-09-16 11:18 EDT (bjork): two appointments written by one batch call → "⚠️ Plan stopped before finishing: 0/2 steps verified" under a correct reply; the stringified properties were saved only by the runner's lenient parse | medium | fixed | 3 | 2026-09-16 | codex/tool-catalog-file — the relationship is DATA: `batch_of: {tool: life.observe, items_pointer: /observations}` in `catalog/tools.yaml`; `plan_eval::expand_batch_calls` adds one member call per item (per-item outcome from `results[]` / `evaluation.rejected[]` / `not_attempted[]`) after the kept batch call; `tool_args` repairs each item against the member schema |
| DEF-148 | Event/appointment times stored in UTC for local wall-clock times, and the correction did not land. 2026-09-16 11:18 EDT bjork wrote `occurs_at: 2026-09-16T19:00:00Z` for "7:00 PM" (invented — the source said only "tonight") and `2026-09-20T09:00:00Z` for "9:00 AM" (3 PM / 5 AM EDT in the graph); the prompt carries `local time … EDT (America/New_York)` but the `life.observe` `occurs_at` description never asks for an offset. At 11:29 EDT the operator corrected both; bjork sent NO `occurs_at` at all (she retitled the practice "(No Set Time)"), the runner keeps a stored date when the new one is empty — so an empty value can never CLEAR a date — and she replied "durably set for 9:00 AM Eastern". (Earlier note that observe cannot change a confirmed node's dates was WRONG: `ON MATCH` overwrites `occurs_at` whenever a value is sent.) Graph repaired 2026-09-16 17:00 UTC through an operator-channel bjork turn (practice `2026-09-16`, warmup `2026-09-20T09:00:00-04:00`), verified in Memgraph | high | open | 3 | 2026-09-16 | still owed: `occurs_at` description in catalog/tools.yaml requires the operator's UTC offset and forbids inventing a clock time the source did not give (date-only instead); a way to clear a structured date; the claim gate should catch "set to <time>" when no call sent that date |
| DEF-152 | Cron jobs for an agent role whose payload carries no `message`/`content`/`paracrine_signal` fire into nothing: `build_cron_task_json` only promotes `message`/`content` into the task's `content`, and the philote's `handle_user_message` returns early when `normalized_user_content` is None — no turn, no log line. Live 2026-09-16 19:32 UTC Beacon registered the operator's daily gardening review as `{"action": "lifegraph_gardening_review"}` and told the operator it was scheduled. Two older enabled vps jobs had the same shape and zero turns ever: `069f52a1` `{"action": "workout_reminder"}` (weekdays 21:40 UTC, since 2026-08-26) and `1a0c3dea` Chronos `{"action": "whisper_practice_update", …}` (daily 13:00 UTC, since 2026-09-13), both seen firing in the hotel log | high | fixed | 2 | 2026-09-16 | `codex/beacon-skill-cron-review`: `cron_payload_reaches_an_agent` (shared with the fire path); `RegisterCronJob` refuses a `role:` target without one (`CRON_PAYLOAD_UNDELIVERABLE`, message shows the required shape); a firing job without one logs a warning instead of vanishing; the `cron.register` catalog schema says the instruction goes in `message`. The three existing jobs are NOT rewritten — the operator/Beacon should re-register or remove them |
| DEF-153 | `life.list` gave no sign that a page was capped: `count` was the number of rows returned and the default limit is 50. Live 2026-09-16 19:38 UTC Beacon rendered a "review of all proposed items" from one 50-row page (15 items shown) while 197 proposed nodes existed across 21 labels (26 Person alone) | medium | fixed | 1 | 2026-09-16 | `codex/beacon-skill-cron-review`: results carry `limit` and `truncated`, plus a `note` when the page is full; catalog description states default 50 / max 200 and that a truncated page is not the complete set |
| DEF-156 | Two say-do gate misses in Beacon's 2026-09-17 morning. (1) The LifeGraph runner's reminder heartbeat hands Beacon "Heartbeat reminder dispatch … Send the operator this reminder text as one Telegram message, verbatim" with `source: telegram`; at 12:31 UTC her correct plain-text reply was sent back by the plan gate as an unplanned request, she declared a plan she could never settle, and the reminder reached the operator three times followed by "⚠️ Plan stopped before finishing". (2) At 12:57 UTC a zero-tool turn replied "I am taking care of those links in the background right now" and no say-do check fired — the phrase matched no promise pattern | medium | fixed | 1 | 2026-09-17 | `codex/beacon-morning-gates`: `is_heartbeat_reminder_dispatch` exempts the runner's fixed-opening delivery turn from the plan gate (the runner test pins the opening); the promise patterns learn "taking care of that/those/it", "in the background right now", "I'm handling". Same morning, reported not fixed: a reply cited `life:open-loop:7b9de2525e7cc06a` and `…b01e8082924968e9` as "retired/merged" with no write call on them and no correction fired (DEF-149, owned elsewhere) |
| DEF-155 | The hotel's refusal of a peer delegation never reached the model: `delegate.to_peer` matched `IpcResponse::Error` and `DelegationAck` but not the standard failure envelope (`Standard { ok: false, code, message }`), so DEF-139's `DELEGATION_UNROUTABLE` explanation fell through to `Ok(_)` → "delegate.to_peer: unexpected hotel response". Live 2026-09-17 02:24-03:00 UTC: Beacon addressed the peer as `bjork` (the agent id is `agent-bjork-01`), three delegations were refused, and she told the operator they hit "transient IPC connectivity failures" and were "queued to go out as soon as the mesh gateway clears" - nothing was queued | high | fixed | 1 | 2026-09-17 | `codex/peer-delegation-refusal-visible`: the arm renders the hotel's code and message (`DELEGATION_UNROUTABLE`, not `UNEXPECTED_RESPONSE`); the refusal lists the peer agent ids this hotel can see and says nothing is queued. Same-shaped gaps exist in other tool arms (`role.configure`, `skill.register`, `procedure.*`) - not touched here |
| DEF-154 | A plan made of reply-rendering steps looped: `split_bundled_steps` split the prose step "Render a clean, structured review grouped by type with individual and batch validation mechanisms." into "Render a clean" / "structured review … individual" / "batch validation mechanisms."; the model then re-declared them bound to `text.generate`, which no tool call can prove, so they came back `contradicted` on every continuation. Live 2026-09-16 19:38–19:39 UTC: four `life.list` calls, four near-identical dashboards sent to the operator, then "Plan stopped before finishing" | medium | fixed | 1 | 2026-09-16 | `codex/beacon-skill-cron-review`: `atomicity_violations` (and so the splitter) considers only tool-bound steps; `set_active_plan` unbinds reply pseudo-tools (`text.*`, `respond`, `render`, …) so such steps settle on the model's word. Replay test |
| DEF-151 | A `delegate.to_peer` delegation crossed the mesh and was dropped on arrival: the envelope's `target_agent_id` is the peer AGENT id ("agent-bjork-01"), but `deliver_event_envelope_or_park` treats that field as a role name, and no guest subscribes a role by an agent's id (Björk's orchestrator subscribes `agent` as guest `agent-bjork-01:orchestrator`). Live 2026-09-16 18:40 UTC: the operator told Beacon "Looks like you did not do a peer delegate call"; she called `delegate.to_peer` (ack "dispatched", 📨 "not confirmed" trailer correctly attached, DEF-141 proven live); mac-jane logged "Cross-hotel task ad9645de…: no subscriber for role 'agent-bjork-01', no specific guest; task dropped." one second later — the first time a peer delegation ever reached the target hotel (DEF-139/140 proven live) | high | fixed | 2 | 2026-09-16 | `codex/peer-delegate-inbound-routing`: when no subscriber matches and no guest is named, `agent_addressed_subscriber` resolves an agent id to its live orchestrator (`agent` role guest `<agent>:orchestrator`, else `role:<agent>:orchestrator`, else any `<agent>:*` agent guest) and delivers there; role names and unknown agents are untouched. Test. Open: the delegation carries no reply route back to the sender, so Björk answers through her own hotel's default reply path, not to Beacon |
| DEF-150 | A refused `delegate.whisper` read as success to every history gate, and Beacon told the operator it had been delivered. Live 2026-09-16 18:16–18:17 UTC: the operator re-sent five items; all six `life.observe` calls landed (the DEF-144/146 fixes held), then `delegate.whisper` to `virtuosa` was refused (`SPECIALIST_UNAVAILABLE`, whispers are hotel-local and virtuosa lives on mac-jane). The runtime emitted `step_failed`, keyed on the result's error payload, but `handle_tool_result` stored only the prose "delegate.whisper failed: specialist role 'virtuosa' is unavailable …", which opens with the tool name, so `distill::tool_result_is_error` read it as ok. The reply said "I have routed a one-line whisper over the mesh to Björk … She'll review your practice details automatically" with no correction. Even with the failure visible the gate would have stayed silent: "routed" was not a completed-work verb, and the delivery-claim detector from DEF-141 only ran on zero-tool turns | high | fixed | 2 | 2026-09-16 | `codex/philote-failed-step-agreement-2`: `tool_result_content_with_error` appends ` \| kind=… \| code=…` to a result that carries an error payload but does not already read as one, so the history classification and the `step_failed` event agree for every tool, not just the whisper; `reply_reports_completed_work` learns the messaging verbs (routed, notified, whispered, sent a/her/them, handed off, relayed…); `reply_claims_unbacked_delivery` joins the claim-over-failure test. Replay test from the live turn. Not touched: DEF-149 (claim audit, owned by a parallel session) |
| DEF-149 | Claim audit launders false receipts: `unbacked_cited_ids` treats any id present in the recent dialogue as backed, and the pre-DEF-146 false `✅ Written this turn: observed life:event:sacrament_music_20260920` receipts put that id into bjork's own earlier replies, so at 2026-09-16 11:18 EDT she said the event was "already settled into your LifeGraph" (it does not exist) and no correction fired | high | open | 2 | 2026-09-16 | an id in the ASSISTANT's own past text must not back an existence claim; only tool results (this or prior turns) and operator messages do |
| DEF-131 | (same defect as DEF-130, filed from the 14:10 UTC vps ledger, which was on the #515 build — #514 had not been deployed there) The closing gardening step "Re-run life.audit…" could not be verified by an argument-less `life.audit {}` call. #516 added an order-blind "unique-tool" pass for it, so the audit that SEEDED the plan satisfied the closing step: watched-live 2026-09-15 16:15 UTC the pass reported 13/13 complete with no second audit. #516 also regressed two DEF-130 tests and was merged with its PR Check pending (develop red until #517 narrowed the pass to read-only tools with the DEF-130 call-order floor) | high | fixed | 2 | 2026-09-15 | `codex/philote-closing-audit-fence`: `SessionState::plan_evidence_from` (calls made before a reflex seeded the plan are not its evidence) now also applies at the turn-end evaluator (`plan_followup_after_turn` re-verified from the full working history and bypassed the fence `push_tool_history` honours); regression test replays the live turn. Kept from #516: `outstanding_briefs` in `plan_eval` + named outstanding steps in the status trailer; dialogue-aware claim audit |
| DEF-132 | A handoff to a role whose `home_node` holds the bare hotel NAME (records that predate DEF-124: bjork's theoretician, stamped 2026-09-07, `home_node: "mac-jane"`) was dispatched "remote" — `handle_handoff_to_role` compared the raw string to the local node_id (`mac-jane-aiua-01`), appended a mesh event for a peer that does not exist, set the session's active incarnation to the role, and the operator's thread went silent. The follow-up `EmitTask` hit the same value through `resolve_guest_home_node` ("auto-routing to mesh peer remote=mac-jane" → "target node unknown to this hotel — task may never deliver"). Live 2026-09-15 14:44 UTC, mac-jane, after "Handed off to role 'theoretician'" | high | fixed | 1 | 2026-09-15 | codex/handoff-home-node — both sites resolve the home through `resolve_hotel_node_id` (node_id or hotel_name → node_id) before comparing/routing; IPC test: a role homed by the local hotel's NAME receives the bundle locally. Live record patched on mac-jane (`home_node` → `mac-jane-aiua-01`); mbp-jane/vps-jane hold no bare-name homes |
| DEF-133 | `rule.propose` ran twice for one operator ask: the plan step was re-executed on the continuation after the first `/approve` (10:44:15 EDT), asking for approval again; the second `/approve` (10:44:37) created an identical rule. Two `Always use the music.repertoire-gardener skill…` rules landed 22 s apart | medium | open | 2 | 2026-09-15 | approval-completed steps must be marked verified from the approval result, not re-planned; the duplicate rule row was removed by hand on mac-jane |
| DEF-134 | A role guest seeded from mesh-config (`PHILOTIC_ROLE_NAME` only, no `PHILOTIC_ROLE_INBOX` — e.g. `mac-jane:philote-theoretician`) registered under the bare role name `theoretician`, but `is_agent_handoff_caller` accepts only `agent` or the routing role `role:{agent}:{role}`, so the incarnation could receive a handoff yet never hand back: live 2026-09-15 16:06 UTC bjork's theoretician got HANDOFF_FORBIDDEN on `handoff.back` and the operator's thread stayed parked on the role | high | fixed | 1 | 2026-09-15 | codex/role-inbox-default — philote defaults its registration role to `role:{agent_id}:{role_name}` when the inbox env is absent (`role_registration`, 3 tests); the hotel gate also accepts the bare role name from pre-fix binaries (test). Live: the Telegram session's active incarnation reset to the orchestrator by DB patch |
| DEF-139 | `delegate.to_peer` was a phantom on every hotel: the IPC handler built the mesh envelope with `target_node_id: None` ("router will resolve") and acked `status: dispatched`, but the ledger writer treats a node-less envelope as same-hotel and skips it — never stored, never routed, never delivered, no log line. Live 2026-09-15 19:35 UTC: two delegations from Beacon to `agent-bjork-01` acked "dispatched"; neither corr_id exists in either hotel's `mesh_events`; mac-jane received nothing; Beacon told the operator Björk had received them | high | fixed | 3 | 2026-09-15 | `codex/peer-delegation-routing`: the handler resolves the target agent's authority hotel to a mesh node (`resolve_hotel_node_id`) BEFORE acking and refuses with `DELEGATION_UNROUTABLE` when no hotel is known to host the agent — falling back from the local graph's agent identities (only agents THIS hotel materialized) to the gossiped peer rosters in the `NodeRegistry` (`peer_agent_node_from_roster`, follow-up PR after #526); the ledger writer logs a warning for any agent-addressed envelope without a node instead of silently skipping it; IPC test |
| DEF-140 | mac-jane's proactive hotel-state broadcast fails every 30 s with "Message too long (os error 40)": the legacy integer-array wire encoding wraps its ~3 KB roster (46 guests) to ~10 KB, over macOS's 9216-byte `net.inet.udp.maxdgram`, and the chunker cannot split the roster — so no peer ever learns mac-jane's agents (the vps journal has zero "Hotel state sync from mac-jane" lines), which is exactly why `agent-bjork-01` was unroutable from vps-jane in DEF-139. Dual-read base64 decoding has been on every hotel since 2026-07-04 (7f4bb881) but the sender default stayed legacy | high | fixed | 1 | 2026-09-15 | `codex/peer-delegation-routing`: base64 is the default beacon payload encoding (`PHILOTIC_BEACON_PAYLOAD_B64=0` pins legacy); no receiver change needed. **Proven live 2026-09-16 02:10 UTC** in the vps hotel log (`/opt/philotic/data/logs/aiua.*.log` — the hotel's own tracing is NOT in journald): "Hotel state sync from mac-jane" 1× all day before the change, every 30 s after ("45 guests, 4 agents") |
| DEF-141 | Beacon reported a peer delegation as received: `delegate.whisper` failed honestly (`SPECIALIST_UNAVAILABLE`, whispers are hotel-local and her Muninn memory "reachable via delegate.whisper role=virtuosa, the mesh routes it" is false), `delegate.to_peer` acked "dispatched", and the replies said "successfully dispatched … she has received the update"; a later turn with no tool call at all said "Our system logs confirm that the dispatch went through instantly" | medium | fixed | 1 | 2026-09-15 | `codex/peer-delegation-routing`: the ack text says "queued on the mesh — delivery is NOT confirmed"; `peer_delegation_trailer` appends "📨 Peer delegation to `agent`: queued on the mesh — delivery is not confirmed" to any reply from a turn that queued one; `reply_claims_unbacked_delivery` joins the say-do gate so a zero-tool turn claiming delivery is re-entered once, then corrected. Beacon's Muninn engram is the operator's/her own to evolve |
| DEF-142 | Skill projection was decided by COMPILED tables keyed by skill name (`catalog::tools_for_skill`, `skill_implied_tools`, `skill_is_relevant_for_turn`), so every skill registered at runtime — every skill a philote or the distill lane authors — had no tools, was never relevant, and never projected, whatever its assignment or the rules naming it. Live 2026-09-15 14:24 EDT: the rule "always use music.repertoire-gardener" fired on a turn where the skill was absent from projection; the only path that reaches a skill by name is `subagent.spawn skill_name=…`, so bjork spawned a tool-less worker. The hotel already expanded tool grants from records; philote's per-turn projection ignored them | high | fixed | 2 | 2026-09-15 | codex/skill-projection-from-records — hotel ships `effective_skill_records` (projectable records of the assigned SkillDAG closure + on-demand) in the bindings; philote ingests them and projects from the record: declared `implied_tools` on intersection (assigned) or intersection + relevance (on-demand); records without declared tools project when relevant (skill-name part, ≥2 description/goal words, or an active [Rules] entry naming the skill) and a mentioned tool is projected or a rule names it. Compiled tables stay as seeds. 3 tests from the live shapes |
| DEF-138 | Gardening pass, watched-live 2026-09-15 19:22–19:23 UTC (#522 build): turn 1 6/13 (honest trailer), continuation 12/13, harness closer ran the audit (62 → 63) — then the model re-ran all twelve landed links and the reply carried no health line. Three causes: (1) a continuation turn installs the carried plan with step statuses but never its evidence, so `reentry_hint`, the integrity note and the duplicate-call guard all told the model "1/13 verified, still outstanding: steps 1–12"; (2) the "[Plan complete]" note went out beside 26 tools because the tool-result re-entry envelope projects tools through `project_tools_for_turn`, not the initial-call path #519 gated; (3) `gardening_health_trailer` bailed on the last audit entry being a "[Duplicate call skipped]" text result | high | fixed | 2 | 2026-09-15 | `codex/philote-continuation-evidence`: `CarryoverPlan::seed_turn_plan` returns the plan AND its verified flags and the continuation turn opens with both; `project_tools_for_turn` returns no tools once `plan_fully_verified` (every path); the trailer skips non-JSON audit entries. This is the mechanism behind every "re-ran the completed tidies" observation since slice 2 |
| DEF-137 | Gardening pass, watched-live 2026-09-15 18:30 UTC (#519 build): two of the twelve seeded link steps targeted non-`life:` ids (`ontology:extensions`, `pref-bjork-manages-musician-roles`), so their only `life:` id was the shared anchor `life:role:chief-of-staff` — which every tidy call names. The exact-id pass credited them with the organ-practice and help_daxton calls, those two steps (called, "tidied") were reported outstanding, the in-turn latch drifted to a false 13/13 ("[Plan complete]" at 18:30:38 while the turn-end eval said 11/13), and the continuation re-ran three already-applied links; only one audit ran, so no health line | high | fixed | 2 | 2026-09-15 | `codex/philote-shared-anchor-ids`: `verify_plan_steps` Pass 0 ignores ids shared by two or more steps (a step left with no distinguishing id falls through to the token passes); replay test from the live fixture asserts batch and incremental verdicts agree; `life.audit` proposes SCOPED_TO links only for `life:` ids, lists other-scheme orphans under `needs_judgment`, and treats `OntologyExtension` as a system label |
| DEF-136 | Gardening continuation, watched-live 2026-09-15 16:33–16:34 UTC (#516 build): the harness closer ran `life.audit` (59 → 61) and every step verified, but the re-entry still offered the model its tools and the continuation brief listed each completed tidy with its full call payload — the model re-ran all twelve links (each returned `tidied` again because the link Cypher's MERGE cannot tell a matched edge from a created one), the write receipt listed the twelve re-links as "Written this turn", and the final reply ("No further structural tool calls are required") never reported the score delta the closing step asked for | medium | fixed | 3 | 2026-09-15 | `codex/philote-closing-audit-fence`: completed steps in the brief are listed without their `: call …` payload; a fully verified plan (`SessionState::plan_fully_verified`) re-enters with NO tools and a "[Plan complete] … reply with the final report" note; the link Cypher returns `created` and the provider reports `already_applied` for an existing edge; `uncited_writes` skips `already_applied` results; the gardening plan carries the seeding audit's `health_score N before` in its goal and closing step, and `gardening_health_trailer` appends "📊 LifeGraph health: 59 ↑ 61, live orphans …, duplicates …" from this turn's re-audit (harness-owned measurement, never the model's word) |
| DEF-135 | The philote's `handoff.back` / `handoff.to_role` arms treated a plain failure envelope (`Standard { ok: false, code, message }`) as `UNEXPECTED_RESPONSE: unexpected hotel response`, hiding the hotel's actual refusal (HANDOFF_FORBIDDEN) — bjork then told the operator the tool "timed out" / "hit a network hiccup" | medium | fixed | 1 | 2026-09-15 | codex/role-inbox-default — both arms carry the hotel's code and message through as an `ipc_failure` |
| DEF-136 | `plan_eval::tool_result_looks_ok` only rejected results opening with `error`/`{"error"`/`tool execution failed`; the hotel's own refusals open with prose (`only orchestrator or management guests may registering skills \| kind=ipc_failure \| code=REGISTER_FORBIDDEN`), so a refused `skill.register` VERIFIED the plan's only step and the plan was declared complete. Live 2026-09-15 16:35 UTC (bjork, operator-chat) | high | fixed | 1 | 2026-09-15 | codex/claim-gate-promise — `tool_result_looks_ok` also defers to the distill lane's `tool_result_is_error` (`\| kind=` payload marker, wholesale batch rejections); test from the live text |
| DEF-137 | A PROMISE over an unretried failed step slipped the claim gate: after the refused `skill.register` the reply ended "I will register this now." — not a completion claim, so `reply_claims_over_failed_steps` passed it, and the turn ended with nothing scheduled to run | medium | fixed | 1 | 2026-09-15 | codex/claim-gate-promise — the gate also fires on `reply_promises_unexecuted_action`; `I will/I'll <register\|update\|apply\|fix\|handle\|do> this/it/that now`, `registering this now` added to the promise vocabulary; test from the live reply |
| DEF-130 | Apple agent selection could publish delayed history under a newer recipient | medium | fixed | 1 | 2026-09-14 | `codex/apple-companion`: clear old conversation synchronously, revision-gate async selection, and check recipient at send; A-B-A revision regression test. Found by code review; no claim of observed misdelivery. Seam: `device-tool-plane`. Initial local commit c3728c37 referenced DEF-108 before discovering that develop already used that ID; DEF-130 is the canonical ID for this fix. |
| DEF-001 | Hotel-scoped capability advertisement off-by-one (inactive tool-runner counted) | low | open | 1 | 2026-03-10 | — |
| DEF-002 | Abstract tool storage methods unwired in sqlite storage (legacy `ansible` crate Gap 3) | medium | fixed | 2 | 2026-03-10 | port to `ansible-mesh-core` (methods live in `domain/mod.rs`) |
| DEF-003 | `aiua` binary test target could not compile/run | medium | fixed | 2 | 2026-03 | mock stubs + fallback fix (pre-04) |
| DEF-004 | Multi-tool synthesized response not surfacing | high | fixed | 2 | 2026-03-30 | superseded by cognitive loop v2 + resilient loop (PR #58, #59); no recurrence since |
| DEF-005 | `aiua` test suite hangs on 10 desktop-membrane/e2e tests | medium | fixed | 3 | 2026-06-22 | codex/aiua-test-unhang |
| DEF-006 | Watchdog evicts WaitingModel turns instead of escalating fallback tier | high | fixed | 1 | 2026-06-27 | PR #93 (2e33928) |
| DEF-007 | Voice routing lost on checkpoint restore (`agent_profile` not serialized) | medium | fixed | 1 | 2026-04-29 | PR #60 |
| DEF-008 | `IpcResponse` untagged-enum ordering: `UserProfileData` swallowed `MemoryConfig` | medium | fixed | 1 | 2026-04-29 | wrapper struct + `deny_unknown_fields` (2026-04-29) |
| DEF-009 | Gemini no-content SSE fell back to un-timed batch call — 27-minute hang | high | fixed | 2 | 2026-05-04 | PR #59 (batch fallback removed, 8s idle timeout) |
| DEF-010 | Gemini keep-alive "thinking-drip" resets SSE idle timer — unbounded stream | high | fixed | 1 | 2026-05-20 | f7f8715 (wall-clock cap); 816dadd (forwarder deadlock) |
| DEF-011 | membrane-mcp orphan storm — ghost processes fight one lease | medium | fixed | 2 | 2026-05-29 | 8abef40, 18cbf14 |
| DEF-012 | `hotel.status`/`hotel.logs`/`role.set_home` had no execution route | low | fixed | 1 | 2026-06-01 | `is_local_agent_tool` additions |
| DEF-013 | String `completed_at` killed whole session snapshot via `?` propagation | medium | fixed | 1 | 2026-06-01 | warn+skip in `list_session_turns` + data surgery |
| DEF-014 | Unbuffered OOB hotel broadcasts broke `send_request` (Telegram seats dark) | medium | fixed | 1 | 2026-06-04 | 63f7072 |
| DEF-015 | Paracrine replies silently dropped (`reply_to_guest_id` = membrane seat); delegate.merge unrouted | high | fixed | 2 | 2026-06-08 | d0b969e, 542d967 |
| DEF-016 | `mesh_events` table grew unboundedly (84k stale events found) | medium | fixed | 1 | 2026-06-12 | a2ed50d |
| DEF-017 | Cron jobs to dormant role incarnations silently dropped (bare `target_role`, no materialization) | high | fixed | 3 | 2026-06-19 | PR #80 |
| DEF-018 | `UserProfileData` classified as ignorable push — guest request hang | medium | fixed | 1 | 2026-06-19 | PR #81 (4687ae4) |
| DEF-019 | Agent responses parked ledger-only when `active_incarnation_id` guest not live | high | fixed | 2 | 2026-06-22 | PR #84 + #85 |
| DEF-020 | LifeGraph cross-hotel TaskInvoke parked forever (90s watchdog on every call) | high | fixed | 2 | 2026-06-19 | PR #86 |
| DEF-021 | Conversational-goal gate: `"ok"` substring-matched inside `"look"` — zero tools for turn | medium | fixed | 1 | 2026-06-24 | PR #87 (ab1ce23) |
| DEF-022 | Zombie `running` turns accumulate after restart storms (64 found on bjork) | medium | fixed | 2 | 2026-06-27 | PR #91 |
| DEF-023 | Role handoff delivered zero context and never auto-executed | high | fixed | 2 | 2026-06-27 | PR #92 |
| DEF-024 | `brain` role incarnation loops infinitely on mac-jane, never returns reply | medium | open | 3 | 2026-06-27 | — |
| DEF-025 | Boot-time fallback mesh port persisted as advertised port — hotel silently diverges from peers | medium | open | 2 | 2026-07-01 | peer-side mitigation only (94edd08) |
| DEF-026 | Provider secrets leaked into Muninn config payload | high | fixed | 1 | 2026-07-01 | PR #95 (bf5bee6) |
| DEF-027 | ElevenLabs voice synthesis and Telegram streaming broken | high | fixed | 2 | 2026-07-02 | PR #98 (c5e6263) |
| DEF-028 | Telegram poll lease lost on network flap — seats dark up to 600s | high | fixed | 2 | 2026-07-02 | PR #99 (648acf9) |
| DEF-029 | Approval inline buttons never resolved parked turns (300s timeout, 7 occurrences) | high | fixed | 1 | 2026-07-02 | PR #99 (1b86a45) |
| DEF-030 | `/role` to current role triggered infinite self-handoff loop | high | fixed | 2 | 2026-07-02 | PR #101 + #102 |
| DEF-031 | TTS replies delivered as music cards instead of Telegram voice notes | low | fixed | 1 | 2026-07-02 | PR #102 (36e73c2) |
| DEF-032 | `HotelStateSync` UDP broadcast EMSGSIZE every 30s (real ceiling: macOS maxdgram 9216 + 4x int-array wire inflation) | medium | fixed | 3 | 2026-07-03 | PRs #104/#107/#109 + fleet flip `PHILOTIC_BEACON_PAYLOAD_B64=1` (2026-07-04, watched-live-green: 0 EMSGSIZE post-flip; vps receives mac roster) |
| DEF-033 | `FailTask` never updates persisted `session_turn` status (record-keeping gap) | low | open | 1 | 2026-06-22 | — |
| DEF-034 | router-listener crashes on startup — missing `router_listener.config` DB key | medium | open | 1 | 2026-06-04 | — |
| DEF-035 | `push-homebrew-remote.sh` hand-starts launchd-managed hotels, orphaning them from supervision (stale `active_pid` rows then block re-bootstrap) | medium | fixed | 1 | 2026-07-06 | codex/deploy-launchd-logs (launchd-aware restart: detect label by pattern, clear `hotels.active_pid`, kickstart/bootstrap; hand-start only when no service) |
| DEF-036 | `aiua.log`/`aiua.err.log` unbounded growth — launchd `StandardOutPath` never rotates (61MB on 2026-07-06; 952MB historic on mbp-jane) | medium | fixed | 1 | 2026-07-06 | codex/deploy-launchd-logs (`scripts/install-log-rotation.sh`: newsyslog drop-in, 50MB/keep 5/compressed, run per-push, graceful without sudo; Linux hotels use journald — untouched) |
| DEF-037 | Literal substring `"life.observe"` in a cron prompt hijacked the direct-command shortcut, short-circuiting the whole chartered model pass and writing junk OpenLoop/Signal nodes | medium | fixed | 1 | 2026-07-08 | PR #169 (codex/direct-observe-guard) — shortcut now gated on task origin (`cron_job_id`/`transport=cron`/`source=cron-ticker`/paracrine signal never take the parser path) |
| DEF-038 | Vault root-key resolution order was context-dependent (keychain-first): launchd/ssh contexts silently fell through to the file key while GUI shells resolved the keychain key, so the same hotel used different keys depending on how it was started — bricked gemini/elevenlabs twice (2026-07-04, 2026-07-08) | high | fixed | 1 | 2026-07-08 | PR #170 (codex/vault-key-source-determinism) — deterministic env -> file -> keychain order regardless of execution context |
| DEF-039 | `push-homebrew-remote.sh` probe captured `rc=$?` after an `if` with no `else` (always 0) — every genuinely-new binary (first hit: `model-controller-anthropic`) misclassified as unreachable, aborting the push mid-stream and leaving alphabetically-later binaries (incl. `philote`) stale | high | fixed | 1 | 2026-07-08 | PR #171 (codex/push-probe-exit-code) |
| DEF-040 | `ProviderConfigs::load` hard-failed the entire provider-config refresh on the first undecryptable vault secret, taking down ALL providers instead of just the affected one (one stale `openai_api_key` row broke Gemini turns) | high | fixed | 1 | 2026-07-08 | PR #172 (codex/provider-refresh-isolation) — per-provider key/oauth fetch isolated; a failed fetch degrades only that provider to unconfigured |
| DEF-041 | Turn-level failures (provider errors, watchdog evictions, fallback-ladder exhaustion) terminated silently instead of feeding the self-heal queue | medium | fixed | 2 | 2026-07-08 | PR #173 (codex/turn-failure-heal-intake) |
| DEF-042 | Tool-result deliveries (e.g. `life.*` invokes) poisoned session placement provenance, causing 6/6 forensically-traced turns to die at the 90s watchdog because the tool's own result was misread as the session's persisted delivery target | high | fixed | 2 | 2026-07-08 | PR #174 (codex/tool-delivery-provenance) — only genuine agent-turn deliveries may update `agent_runtime_provenance` |
| DEF-043 | Telegram sent two identical replies per turn: cumulative `partial_reply` drafts converge on the final text, `editMessageText` 400s "message is not modified", and the silently-treated-as-failure path fell back to a fresh `sendMessage` without deleting the stale draft | medium | fixed | 1 | 2026-07-08 | PR #177 (codex/telegram-double-reply) — "not modified" treated as success; stale drafts deleted before any fallback send |
| DEF-044 | `ConfigureRole` / `role.create_or_update` unconditionally reset `fallback_tiers: Vec::new()` on every call with no way to set a ladder via IPC — every reconfigure silently wiped DB-edited fallback ladders (e.g. mac-jane's orchestrator losing its `model.openrouter` tier) | high | fixed | 1 | 2026-07-08 | PR #179 (codex/configure-role-ladder) — optional `fallback_tiers: Option<Vec<String>>`, wire-compatible; `None` preserves, `Some` sets after validation |
| DEF-045 | `philotic-client` IPC has no request/response correlation: a `tokio::time::timeout`-abandoned `send_request` leaves its reply queued on the stream, permanently desyncing the connection one frame — every subsequent request reads its predecessor's response (live symptom on agent-jane: `SyncApartment` receiving `sync_session_index`'s `ConfigData`). Philote wraps dozens of `send_request` calls in external 5s timeouts; any one firing after the write poisons the connection until process restart. membrane-mcp built its own correlation (d3f403c) but the SDK never got it | high | fixed | 3 | 2026-07-10 | PR #229 (codex/ipc-response-correlation) — `send_request_with_timeout` + stale-frame skip-counter (OOB broadcasts excluded from stale-consumption); 31 call sites migrated; wire-compatible. mbp-jane still needs the redeploy |
| DEF-046 | mbp-jane's reported "heal-dispatcher recurring-restart instability" was misdiagnosed at the guest level: `aiua.err.log` on mbp-jane shows ~9.6k untimestamped `Error: Hotel 'mbp-jane' is already running with PID <N>. Stop that instance before starting another.` (the `crates/aiua/src/main.rs` startup guard, ~line 7411), including 8076 hits against one long-lived PID and 1522 against its successor — evidence of a repeated *second* `aiua --hotel mbp-jane` launch attempt colliding with a genuinely-running instance, roughly every ~10s during the worst stretch (2026-07-10, 1532 boot attempts in one day). Every attempt (successful or refused) first logs "Hotel booting with N seeded guest(s)" — which re-lists heal-dispatcher — before the PID guard runs, so guest/heartbeat churn from these collisions plausibly reads as "the dispatcher keeps restarting" from the outside, when the dispatcher's own PID only actually changes on a genuine full hotel restart (confirmed once, 2026-07-11T23:47:24Z, clean boot through to a fresh heal-dispatcher PID). Root cause of the *duplicate launcher itself* is still unidentified: no crontab, no matching `launchctl list` entry, and mbp-jane has no `com.philotic.aiua.mbp-jane` job loaded at all (consistent with S1's "nohup, not supervised" finding) — likely a manual/ad-hoc retry loop rather than a registered service. mbp-jane's aiua process is not running at all as of this investigation (2026-07-12T01:19Z) | medium | resolved | 2 | 2026-07-11 | S2 (codex/substrate-s2-heal-the-healer) root-cause pass — no fix landed; needs S1 (launchd KeepAlive) to close the supervision gap, plus identifying and killing whatever issues the duplicate launch attempts **RESOLVED 2026-07-12 (live verification):** the duplicate launcher was launchd itself — `com.philotic.aiua.mbp-jane` IS bootstrapped in the gui domain (invisible to ssh-session `launchctl list`, which caused the "no job loaded" misread), and its KeepAlive retried every ~10s against a manually-started (nohup) aiua holding the instance lock. Storm ended Jul 10 15:57 (err.log untouched since) when the nohup instance died; since then exactly one aiua runs as launchd's own child, and launchd auto-restored the hotel unattended at 2026-07-12T01:20Z (the very window the S2 investigation read as "down"). Supervision invariant now holds; no code fix needed beyond S2's watchdog |
| DEF-047 | `phil` CLI hotel-IPC commands (`autonomy`, `heal`, `keys`, `component`) call `socket_path("aiua")` with the literal string as the hotel name — under `PHILOTIC_PROFILE` this resolves to `<profile>/aiua-aiua.sock`, but hotels create `aiua-<hotel>.sock` (e.g. `aiua-mac-jane.sock`), so every profile-based hotel gets "No such file or directory". Workaround (live-verified): unset the profile and pass `PHILOTIC_HOTEL_SOCKET=<socket path>`. Fix: thread the resolved hotel name (as `start.rs`/`status.rs` do) or glob `aiua-*.sock` in the profile dir | medium | open | 1 | 2026-07-12 | — |
| DEF-048 | Test runs recorded via the MCP `graph_record_test_run` tool do not surface in `phil graph green`, while runs recorded via REST `POST /api/test-run` do (live-verified: REST-recorded 1198/1198 shows green; six MCP-recorded runs from the same evening show `none`) — the two write paths create different TestRun node/edge shapes | low | open | 1 | 2026-07-12 | — |
| DEF-049 | `phil doctor` `logs.rotation-missing` check still looks for the newsyslog drop-in and warns even when the `com.philotic.logrotate` copytruncate LaunchAgent (the current, correct mechanism — newsyslog was retired because rename+bzip2 strands launchd writers) is installed and rotating | low | open | 1 | 2026-07-12 | — |
| DEF-050 | Role-addressed replies broadcast through every Telegram bot seat: a turn with no `final_reply_guest_id` (cron-originated turns never carry one) resolves its reply target to role `membrane` only, and aiua's `deliver_inbound_task` delivers a guest-less task to ALL role subscribers — each seat then posts to the same DM `chat_id` (identical under every bot token), so the operator sees one message per bot. Live-hit 2026-07-13 7:40a ET: aria's evicted dev-brief turn watchdog notice arrived from all 4 mbp-jane bots | medium | fixed | 2 | 2026-07-13 | PR #266 — seat-ownership filter on the session id's terminal agent segment, fail-open for non-Telegram shapes. Deployed mbp-jane + mac-jane 2026-07-13 |
| DEF-051 | Cron-originated turn's model responses silently swallowed — the turn rides the watchdog to a 600s eviction. Forensic (mbp-jane 2026-07-13, first cron fire on the Jul-12 18:47 deploy ≈ PR #260): 7:30a dev-brief turn dispatched to gemini; the response (tool_call `hotel.status`, `model_result: null`) reached aria's turn loop in 1.3s (`action peek` logged) and vanished with NO further log; 7:35a WaitingModel escalation to openrouter, response back in 7.5s, dropped identically; 7:40a CatchAll evicted at 600s. Neither provider hung. The only no-log drop paths in `handle_model_response` are empty `session_id`/`turn_id` and the no-active-turn arm (probe intercept ruled out — probe ids are `probe-<uuid>`; stale-turn mismatch warns). NOT a fresh regression: the identical 11:40:02Z eviction fired on Jul 10, 11, 12 AND 13 — every fire since the dev-brief cron was created, on the pre-#218 binary and the current one alike; the cron turn's model responses have never been consumed. Also hits some live turns (agent-jane evictions Jul 12 00:09/00:39 ×3, Jul 13 14:06Z). Silent-drop instrumentation (PR #272 warn/info on all three drop arms) deployed to mbp-jane philote 2026-07-13 17:51Z — the next 11:30Z cron fire names the guilty guard in the log. DEF-050's fix live-validated at the Jul-13 14:06Z eviction: exactly one seat sent the notice (was 4) | high | fixed | 3 | 2026-07-13 | Root cause: replies addressed by bare `agent_id` land in the base-agent subscription while the turn lives in a role-incarnation runtime (`{agent_id}:{role_name}`) — named by PR #272's instrumentation (2026-07-15 11:30:04Z `no active turn` with the correct turn_id). Fixed across PR #297 (model dispatch stamps `reply_guest_id`), parallel PR #300 (same root cause found independently via paracrine whispers; canonical `model_reply_guest_id()`), PR #301 (tool + life.* dispatches — live re-fire showed the tool RESULT hitting the same misroute at WaitingTool), PR #303 (reconciliation of #300/#301 — #300's merge stranded #301's call sites, develop briefly uncompilable). **VERIFIED LIVE 2026-07-19 21:27Z**: forced cron re-fire on the reconciled build — multi-round tool turn (hotel.status → hotel.logs → …) completed in ~15s, ONE final send via aria's seat, no eviction |
| DEF-052 | Deploy scripts accepted a build tree missing origin/develop commits — a stale-tree `push-homebrew-remote.sh` run silently reverts merged, already-deployed fixes fleet-wide. Live-hit 2026-07-14 13:42Z: a #274-era push from a worktree predating PR #266/#272 reverted the membrane seat filter and the DEF-051 instrumentation on mbp-jane; the 13:58Z aria eviction fanned out through all 4 bots again until a follow-up push from origin/develop restored them (14:25Z) | high | fixed | 1 | 2026-07-14 | `scripts/deploy-freshness-check.sh` sourced by `push-homebrew-remote.sh` + `deploy-mac-jane.sh`: hard abort when HEAD lacks origin/develop (lists missing merges; `PHILOTIC_DEPLOY_ALLOW_STALE=1` overrides; dry-run warns), plus warn-only checks for dirty trees and build artifacts older than HEAD |
| DEF-053 | membrane-mcp listener binds `0.0.0.0` unconditionally at every `ExposureTier` (`crates/membrane-mcp/src/main.rs:161,521`) while `perimeter-core/src/fence.rs:27` documents "Local: loopback-only listener" — Local/Lan tiers apply no listener-level auth, so a `Local`-exposure endpoint (including one with a bearer-token tool) is reachable from the entire LAN. Found in the 2026-07-15 MCP audit; fix = bind-by-tier, slice H1 of `mcp-membrane-hardening` | high | fixed | 2 | 2026-07-15 | codex/mcp-membrane-hardening H1 — bind-by-tier ListenerManager: loopback unless declared exposure > Local AND ceiling honors it; fence on GET /mcp + non-loopback /health; fence docs corrected. test-green, deploy pending |
| DEF-054 | `McpAuthScheme::HmacSha256` is a first-class provisionable config variant but a dead end at runtime: `tools/list` filters such tools invisible (`membrane-mcp/src/server.rs:214,235`) and `tools/call` rejects with generic `PERMISSION_DENIED` (`auth.rs:300-303`) — a valid config silently yields an invisible, uncallable tool. Fix = remove the variant (or implement), slice H3 of `mcp-membrane-hardening` | medium | fixed | 1 | 2026-07-15 | codex/mcp-membrane-hardening H3 — HmacSha256 variant removed; stored configs carrying it fail deserialization with a clear unknown-variant error; bearer grants now self-provisionable via mcp.grant_token |
| DEF-055 | Every philote startup auto-publishes its full `default_toolset` as MCP routes with `auth: McpAuthScheme::None` (`crates/philote/src/runtime.rs:1855-1911` `register_mcp_routes`) through the `UpdateMcpRoutes` IPC path the gateway proposal declares deprecated — silent, unauthenticated exposure of the whole toolset whenever any membrane-mcp guest is running, with no provisioning decision or approval attached. Fix = opt-in publication gate + legacy-path retirement, slice H2 of `mcp-membrane-hardening` | high | fixed | 2 | 2026-07-15 | codex/mcp-membrane-hardening H2 — startup publication opt-in via AgentProfile.mcp_auto_publish (default off, stale route sets actively revoked); derived routes always require_approval. Legacy UpdateMcpRoutes path retirement = step 2, next release |
| DEF-056 | membrane-mcp returns philote business errors as **successful** MCP tool results: `deliver()` forwards `{"error": msg}` as the oneshot payload (`main.rs:277-279`) and the success arm wraps it in a normal ok result with no `isError` (`server.rs:374-381`, legacy `:500-506`); `ToolCallResult::error()` exists and is never called. External MCP clients cannot distinguish failure from data. `StreamingToken` replies are silently dropped (`main.rs:261-264`). Fix = slice H4 of `mcp-membrane-hardening` | medium | fixed | 1 | 2026-07-15 | codex/mcp-membrane-hardening H4 — DispatchOutcome::Err → isError:true results; TOKEN_EXPIRED/ALLOTMENT_EXCEEDED/APPROVAL_REQUIRED specific codes; StreamingToken frames accumulate into empty terminal replies |
| DEF-057 | `seed_toolset_profiles` full-overwrites all 9 seeded profiles on **every** `aiua run`/`aiua load` — any runtime grant mutation (`skill.assign`/`skill.revoke`, operator live-DB patches, runtime-registered `remote_tool_runners` without the env var) is silently wiped on next boot, while `ORCHESTRATOR_MANIFEST` instructs agents to use skill.assign as the capability-growth loop. Found in the 2026-07-19 role/tool deep-dive audit | high | fixed | 2 | 2026-07-19 | codex/role-admin-hardening — `ToolsetProfileRecord::reconcile_seed_with_existing` + stored `seed_baseline`: runtime adds AND removes survive reboot, new seed entries still propagate, `remote_tool_runners` runtime-owned; pre-baseline records treat live diffs as runtime deltas on first reseed |
| DEF-058 | Grant-surface consistency gaps (same audit): (1) orchestrator profile lacked `skill.revoke` — live admins could add capabilities but not remove them; (2) `routing.refinement`/`mcp.manage` referenced in on_demand_skills but never seeded as DB skills (invisible to skill.list); (3) classes `training`/`asr`/`agent_graph`/`mcp` granted in profiles expanded to nothing on both sides (aiua map knew only `life_graph`; philote catalog tags differ); (4) `agent.migrate_to` (only class-`admin` tool) granted to no profile; (5) life-graph runner declared 6-tool catalog vs 8-tool grant surface (PR #271 pattern residue); (6) the `is_admin \|\| role_name=="orchestrator"` authority predicate re-derived inline at 3 gates while `configure_role_record` used `is_admin` alone | medium | fixed | 2 | 2026-07-19 | codex/role-admin-hardening — shared `tools_for_tool_class` map in ansible-mesh-core consumed by both aiua and philote; ghost skills seeded; `skill.revoke`+`agent.migrate_to` granted; runner catalog synced to 8 tools; authority tiers centralized as `has_full_admin_authority`/`has_operational_admin_authority`; regression test asserts every profile-referenced skill exists in the seeded catalog |
| DEF-059 | Cross-hotel `life.observe` writes are lost when the runner hotel restarts mid-turn — at-most-once delivery, no replay after claim; agent confirms capture to the operator but the node never lands (silent data loss + false confirmation). Observed live 2026-07-20 02:24Z: Aria (mbp) captured an operator idea, her 3 life.* tasks hit vps-jane during a `philotic-hotel.service` stop-sigterm window (SIGKILL 02:25:08); recall succeeded pre-kill, observes vanished, Memgraph empty. Predicted by ARIA_IDEA_PIPELINE_PROPOSAL open question (Coach incident 2026-07-14 same class) | high | open | 3 | 2026-07-20 | — |
| DEF-060 | graph-intelligence server bound `0.0.0.0:8900/:8901` with no auth, permissive CORS, and ~15 unauthenticated write endpoints/MCP tools (`server/mod.rs` hardcoded wildcard bind; docs claimed loopback-only). Any tailnet/LAN peer — or any web page via the permissive CORS on JSON POSTs — could mutate the graph, trigger scans, or forge decisions/test runs. Found in 2026-07-20 intel-graph hardening audit | high | fixed | 2 | 2026-07-20 | codex/intel-graph-hardening — default bind 127.0.0.1; `--bind`/`PHILOTIC_GRAPH_BIND` opt-in; refuses non-loopback bind without `--token`/`PHILOTIC_GRAPH_TOKEN` (bearer on mutating REST + all MCP) unless `--insecure-bind`; CORS restricted to localhost origins |
| DEF-061 | Codex harness projection was inert by construction: `apply` wrote only `~/.codex/philotic/harnesses/<id>.json`, which nothing the OpenAI Codex CLI loads ever references (no `~/.codex/AGENTS.md` block, no verify hook) — Codex sessions ran unharnessed since 2026-04 while the graph reported the harness `clean` | high | fixed | 2 | 2026-07-20 | codex/intel-graph-hardening — codex adapter renders a markdown charter at `~/.codex/philotic/harnesses/<id>/AGENTS.md` + merges an idempotent managed block into `~/.codex/AGENTS.md` (loaded by Codex at session start) with a verify-at-bootstrap instruction |
| DEF-062 | `harness verify` hashed only the main projection file — drift in the aux files `apply` writes (`.claude/CLAUDE.md` import, SessionStart verify hook in settings.local.json, windsurf skills/workflows, codex managed block) was invisible; a deleted hook reported `clean` | medium | fixed | 1 | 2026-07-20 | codex/intel-graph-hardening — `aux_checks` recorded at apply (hash for exclusively-owned files, contains-marker for co-owned) and evaluated on every verify |
| DEF-063 | Docs scanner collapsed every `skills/*/SKILL.md` (and `*/README.md`) onto a single node id (`skill:skill`) because node identity used the bare file stem — 32 on-disk skills were represented by 1 graph node, each scan overwriting the last | medium | fixed | 1 | 2026-07-20 | codex/intel-graph-hardening — container-style filenames take identity from the parent dir (`skill:graph-intelligence`, `doc:architecture-readme`) |
| DEF-064 | Harness projection paths derived from the raw CLI argument, so `verify harness:x` and `verify x` addressed **different files** — apply-by-short-id then verify-by-node-id reported phantom drift (and vice versa); live evidence: duplicate `~/.gemini/.../harness:gemini-*` projection dirs and a stray `harness:gemini-local.json`, drift table flipping between invocation styles | medium | fixed | 1 | 2026-07-20 | codex/intel-graph-followups — `short_harness_id` normalization in all target-path/render/marker fns + test asserting both id forms resolve identical paths and content |
| DEF-065 | `AddVaultEntry` (used by `phil keys configure` and agent-migration vault import) hardcoded `secret_kind: "vault-token"`, discarding the caller's `vault_name` — provider keys stored as generic tokens, indistinguishable from MCP token grants in secret_refs when debugging ACL failures; live evidence 2026-07-20 vps-jane provider-key incident (`secret://hotel/default/vault-token/…` refs for gemini/elevenlabs) | low | fixed | 1 | 2026-07-20 | codex/fix-add-vault-entry-kind — kind = `vault_name` at both store sites + regression test |
| DEF-066 | Every `full_scan` silently wiped ALL node embeddings: scans delete+reinsert nodes, and the upsert COALESCE only preserves embeddings when the old row still exists — with 6h scheduled scans, semantic search could never stay populated (live DB had 0 embeddings across 11k nodes) | medium | fixed | 1 | 2026-07-20 | codex/intel-graph-top3 — snapshot_embeddings before clear, restore_embeddings (into surviving ids with NULL embedding) after scan |
| DEF-067 | `memory.hygiene` sweep vault discovery matched zero vaults on every real hotel — `collect_agent_vault_names` (`dream.rs`) filters materialized guest configs for a top-level `agent_id` field, but no guest config in this codebase carries that field at the top level (it lives under `env.PHILOTIC_AGENT_ID`, e.g. `agent_guests_for_profile`); nightly sweeps logged `vaults=0 contradictions=0 stale=0` since first deploy and read as "clean" instead of broken. Live evidence 2026-07-20 mac-jane: `materialized_guests` has zero rows with a top-level `agent_id`, while `config:vault_registry` holds valid `muninn_vault_token` entries for `self_agent-bjork-01`, `self_agent-coach`, `user_likesjx` | high | fixed | 2 | 2026-07-20 | codex/hygiene-vault-discovery — discovery now sources sweepable vaults from `config.vault_tokens` (the resolved `vault_registry`), cross-checked against MuninnDB's own `GET /api/vaults` ground truth; zero-vault discovery now logs WARN with the reason and is recorded distinctly on the last-run marker so an empty sweep is never mistaken for a clean one |
| DEF-068 | `EmitTask` logged and healed an unknown remote target but still returned success and appended an undeliverable event, leaving the caller to time out after a false acceptance | high | fixed | 1 | 2026-07-27 | codex/lifegraph-delivery-assurance — reject with `TARGET_NODE_UNREACHABLE` before ledger append while retaining classified heal evidence |
| DEF-069 | Registered-but-unreachable philote silently swallows every turn. `agent-jane` answered nothing for ~31h (2026-07-25 15:17 → 2026-07-26 22:16Z) after `/sfw` re-pinned `session:telegram:7898847424:agent-jane` to `active_incarnation_id=agent-jane:orchestrator`. That philote was alive, socket-connected and registered, but wholly inert (0.43s CPU / 10.5 MB peak over 36h) and had **never** completed a turn. The hotel created a `session_turn` per inbound message and never dispatched it — no error, no warning — until the 300s zombie watchdog marked it `ZOMBIE_TURN_REPAIR`. Lost operator messages include "Are you there?" and "Dang.". Root cause confirmed: `is_registered("agent-jane:orchestrator")` returned **true**, so `resolve_agent_route` took the `Deliver(Some(active_guest_id))` branch; the base-agent fallback (log string "delivering to its live base agent registration") is present in the deployed binary and fired **zero** times. All safety nets sit downstream of that check. **RECURRED 2026-07-28→29 (~17h, same pin, that philote again inert at 0.54s CPU/17h) — the manual DB patch did not survive, confirming the wedge returns without code.** Two mechanisms identified on the recurrence: (1) **the re-pin needs no `/role` command** — `deliver_live_guest_task` (`ipc.rs:4161`) calls `update_session_active_incarnation` for *any* delivery carrying `activate_session_id`, so a single whisper/parked/role dispatch to a role incarnation silently re-points the conversational session at it; its liveness test is "has an inbox subscription", which the inert philote satisfies. (2) **no detector could fire**: `SUBSCRIBER_BACKLOG_WEDGE_THRESHOLD` is 32 undrained frames, but a turn that dies undispatched contributes ~1 frame, so ~11 lost turns never approach it — `subscriber_wedged` is present in the deployed binary and has fired **zero** times fleet-wide | high | partially-fixed | 3 | 2026-07-26 | recovery half shipped PR #382: the zombie watchdog now demotes a session off a role incarnation that let 2+ consecutive turns die (`decide_role_incarnation_demotion`), bounding a permanent outage to a couple of lost turns and filing `role_incarnation_not_serving`. **Detection half still open** — delivery is still assumed rather than ACKed, and nothing prevents the re-pin onto a non-serving incarnation. See FLEET_SUPERVISION_PROPOSAL.md S2 |
| DEF-070 | Self-heal circuit records `escalated` as `resolved`, so a burning fault reports success forever. `service_probe_failed:muninn` filed **919** entries on mbp-jane at a 4.99-min cadence over 3 days; **918** were written `status=resolved` / `outcome=escalated`. All-time `heal_queue` is 1426/1680 (85%) escalated versus **4** ever `restarted`. The dedup work item (`97ffa94d`) neither suppresses re-filing nor keeps one aging open record. Compounding: every escalation targeted `role:agent-jane:orchestrator`, which was itself the deaf agent of DEF-069 — the alert path ran through the broken component with no out-of-band fallback | high | fixed | 3 | 2026-07-26 | FLEET_SUPERVISION_PROPOSAL.md S4 — `HEAL_STATUS_ESCALATED` + `terminal_status_for_outcome()` so only real repairs claim `resolved`; escalated rows stay terminal for dispatch (never re-picked) and are reaped by the retention vacuum; new `phil doctor` check `heal.escalated-unrepaired` flags the worst still-escalating `(pattern_tag, guest_id)` pair (Warning >1h, Critical >6h). Rows are deliberately NOT collapsed at push — the A3 recurrence tracker counts rows, and a regression test pins that coupling. **Test-green, not deployed.** The out-of-band escalation target (S5) is still open |
| DEF-071 | Muninn daemon unsupervised on both Macs — no launchd job existed for the daemon (only `com.muninn.mcp`, the stdio proxy). It died on mbp-jane 2026-07-23 and stayed down 3 days, leaving every agent on that host memory-blind (`Auto recall skipped: no Muninn memory backend configured`). mac-jane only appeared healthy because `muninn mcp` proxies spawned by Claude clients resurrect the daemon on demand (verified: SIGKILL with no launchd job → fresh `ppid=1` instance in ~6s) | high | fixed | 1 | 2026-07-23 | `com.muninn.daemon` launchd plist (KeepAlive, ThrottleInterval 5) installed on both Macs 2026-07-27, mirroring vps-jane systemd `Restart=always`/`RestartSec=5`; SIGKILL-verified respawn on both (mac-jane 72573→74346, mbp-jane 69942→70039). Covers process exit only; wedged-but-listening still needs FLEET_SUPERVISION_PROPOSAL.md S2+S3 |
| DEF-072 | Proposal nodes created only through the graph MCP do not survive a rescan. Two `proposal:*` nodes plus their `graph_decide` records, filed 2026-07-26 via `graph_create_node`, were gone by the next `intel-graph-freshness` run (2026-07-27 17:57Z) — `graph_create_node` returned success and `graph_decide` accepted writes against them right up until they vanished. All ~110 durable proposals in the graph are `doc:`-prefixed and derived from committed files. This silently undermines the standing rule that proposals belong in the intel-graph rather than in `.md` files | medium | open | 2 | 2026-07-27 | workaround: back every proposal with a committed doc carrying `doc_type: proposal` frontmatter. Real fix = either persist MCP-authored nodes across rescan, or have `graph_create_node` refuse/warn for a bare `proposal:` id |
| DEF-073 | Guest supervisor stale-snapshot race spawned duplicate Telegram gateways. A heal restart cleared `active_pid`; the supervisor captured that state, waited on the materializer lock, then ignored the refreshed replacement PID and spawned again. `LocalProcessMaterializer::children.insert` overwrote the first child handle, leaving an untracked gateway with the same guest IDs. Jane consequently produced one model response but both gateways sent it to Telegram | high | fixed | 2 | 2026-07-28 | codex/jane-double-response — atomic reclaim/respawn under the materializer lock, refreshed-PID recheck before supervisor spawn, and fail-closed duplicate-child guard |
| DEF-074 | Plan step completion was self-certified by the model: `evaluate_plan` trusts `step.status == "done"`, so a step the model marks done without doing it yields a `Complete` verdict and a success report to the operator. Compounded by three things — planning was opt-in behind a keyword allowlist (`looks_like_planning_turn`), the tool-history re-entry hint offered "…**or respond to the user if all necessary work is complete**" which let a turn bail after one tool call, and `plan_stop_notice` was delivered to chat verbatim. Net effect: a five-item request became five round-trips of one-line receipts, then a robotic "*(Plan paused: … /plan drop to discard.)*". Live evidence 2026-07-27 Beacon/vps-jane: asked to add five children to the LifeGraph, Beacon marked the steps done and replied "Yes, I now have all five", but Memgraph had only four `life:person:*` nodes — `daxton_thomas_wagner` was never created. The bundled step ("propose Zerin, Mali and Daxton") was cleared by the one successful `life.observe` that ran | high | fixed | 5 | 2026-07-27 | codex/beacon-plan-execute-evaluate — grounded `verify_plan_steps` (one successful call attributed to at most one step, disambiguated by the tokens unique to each step), `should_plan` inverts the gate to plan-by-default, `plan_directive` requires atomic tool-bound steps, `evaluate_whole_plan` + `plan_integrity_note` bar a turn from claiming completion while a step is unverified, three-exit `reentry_hint` keeps steps running inside one turn under a 210s budget (clear of the hotel's 300s zombie reaper), verification flags latched on `WorkingTurn` outside `active_plan` so model re-emissions cannot erase them, and the stop notice became a `plan_stopped` turn event instead of a chat message |
| DEF-075 | Memgraph (the LifeGraph store) lost its entire database on every restart. `philotic-memgraph.service` ran `docker run --rm` with `--volume /opt/philotic/data/memgraph:/var/opt/memgraph`, but Memgraph's data directory is `/var/lib/memgraph` — so the one persistent mount held nothing (host dir was empty since 2026-06-02) while the real data dir was an anonymous volume that `--rm` deleted on every stop. Live evidence 2026-07-28: the container was recreated at 06:15Z and the LifeGraph went from ~217 nodes (Signal 95, OpenLoop 53, Event 14, Person 6, …) to 15; the pre-wipe `life:person:*` set for the operator's children was destroyed. Silent — nothing alerted, and the agent kept writing into a store that would drop everything at the next restart | high | fixed | 2 | 2026-07-28 | live fix on vps-jane — data dir copied onto the bind mount, `chown 101:103`, unit repointed to `--volume /opt/philotic/data/memgraph:/var/lib/memgraph` (backup at `philotic-memgraph.service.bak-20260728`); durability proven by writing a probe node and surviving two restarts |
| DEF-076 | The plan→execute→evaluate→repeat cycle was grounded on one side only. DEF-074 made the *in-turn* layer refuse to claim completion it could not prove (`verify_plan_steps` / `evaluate_whole_plan` / `plan_integrity_note`), but the *carryover* evaluator that decides whether the cycle repeats still trusted `step.status == "done"` and a non-consuming description-token heuristic. A plan that marked itself done therefore returned `Complete`, dropped the carryover, and exited the loop with the work undone — the DEF-074 failure mode one level up, reachable by exactly the same model behaviour. Three compounding limits: `CarryoverPlan.steps_done` (settable by model claim) was fed back as `prior_done`, laundering self-certification into the next turn's evidence; the first continuation that made no forward progress was `Blocked` immediately, killing plans one turn before recovery; and the continuation budget was a flat 3 regardless of how many steps were outstanding. Separately, `low_progress_tool_name` counted a plan's own declared diagnostic steps as "circling" and truncated legitimate read-only plans | high | fixed | 3 | 2026-07-29 | codex/philote-loop-hardening — carryover eval shares `verify_plan_steps`/`evaluate_whole_plan` with the in-turn layer, evidence carried as id-keyed `verified_step_ids` separate from `steps_done`, `MAX_CONSECUTIVE_PLAN_STALLS` tolerates one stalled continuation, `scaled_continuation_budget` sizes the budget to outstanding work (ceiling 8), the continuation brief cites *why* each step is outstanding (redo / split / change approach), and declared plan steps are exempt from the low-progress stop |
| DEF-077 | The agent loop had no way to speak without stopping. `partial_replies` is plumbed end to end — a model-router channel, extraction in `handle_model_response`, `emit_partial_reply`, and a membrane-telegram `partial_reply` handler that edits a draft message in place — but it was dead at both ends: philote never requested the channel in any `cognitive_response_contract`, and the flush ran only on the `AgentAction::Respond` arm, so partials emitted alongside a tool call were silently dropped. "Say something" and "keep working" were therefore mutually exclusive, which is why a plan could run silent for the full 210s in-turn budget, why the re-entry hint's third exit had to terminate the plan to report a blocker, and why the hint instructed the model not to send progress at all | medium | fixed | 2 | 2026-07-29 | codex/philote-interim-voice — request `partial_replies` on the tool re-entry contract only (not voice), flush one note on the ToolCall path behind `interim_reply_admissible` (iteration and quiet-period gate, stamped in `emit_partial_reply` so every interim path shares one budget), and rewrite the re-entry hint to invite an interim note and to stop the plan only when no other step can move |
| DEF-078 | `scripts/deploy-mac-jane.sh` copied all 29 Cellar binaries to a timestamped `bin-backup-*` directory (~414MB) on every run and never pruned them. 34 had accumulated (~14GB) and filled mac-jane's disk on 2026-07-30. The hotel did not crash and logged no error: `aiua` stayed alive, stopped writing to `aiua.log` entirely, and spawned zero philote guests — every other guest class (model controllers, tool-runner, mcp-client) registered normally, so the symptom read as a philote-specific code regression for a long time. Recovery was pruning the backups and a clean bootout/bootstrap, after which 8 guests came back on the same binary. Two aggravators found alongside: the script silently no-ops when `target/release` is missing (every binary falls through to "leaving legacy binary untouched" while the success banner still prints, so deploying from a never-built worktree installs nothing and reports success), and `com.philotic.aiua-watchdog.mac-jane` respawns `aiua` every 120s, so a `bootout` that is not followed promptly by a `bootstrap` leaves the *old* binary running under the watchdog | high | fixed | 2 | 2026-07-30 | codex/deploy-hygiene-fixes — prune to the newest `PHILOTIC_DEPLOY_KEEP_BACKUPS` (default 3) after each backup, resolve the target dir via `cargo metadata` and hard-abort when no release build is present, and warn when the Cellar volume drops below 5GB free |
| DEF-079 | The hotel's zombie reaper and philote's turn watchdog were mis-layered, so the hotel always killed turns the guest was still legitimately working. `heal-dispatcher` hardcoded `RepairStaleSessionTurns { min_age_secs: 300 }` while `turn_loop::evict_timed_out_turns` used 300s per-phase budgets (`WAITING_MODEL_SECS`, `WAITING_TOOL_SECS`, `WAITING_APPROVAL_SECS`) under a 600s `MAX_TOTAL_ACTIVE_SECS` ceiling, and `PARACRINE_WHISPER_WAIT_SECS` at 660s. Every guest-side budget therefore sat **at or above** the hotel's hard wall: the 600s ceiling was unreachable dead code, a tool wait could never reach its own timeout, and a blocking whisper was guaranteed to be reaped. `plan_eval.rs` already documented the wall as `TURN_ZOMBIE_REAP_SECS = 300` and correctly budgeted plans at 210s to stay under it — but `turn_loop.rs` never referenced it (zero occurrences), so the two modules disagreed about the ceiling they shared. Live evidence 2026-08-05, all three hotels: the turn-duration distribution was **bimodal** — completions at 0–20s, failures clustered at 301–328s, nothing in between — with **46 of 96 turns fleet-wide reaped as `ZOMBIE_TURN_REPAIR`**. On mac-jane four cron turns had produced a full `response_json` and were still marked failed; on vps-jane **18 user-facing turns produced no response at all**, so the operator asked Beacon something and got five minutes of silence. This is the mechanism behind the long-standing "~315s stuck turns", "scheduled briefs never landed", and DEF-068's leaked `model-catalog-sync` turn | high | fixed | 3 | 2026-08-05 | codex/turn-budget-layering — single source of truth in `ansible_mesh_core::turn_budget`: the backstop moves to `TURN_ZOMBIE_REAP_SECS = 660` (above the guest ceiling, so the reaper only catches turns the guest can no longer speak for), `heal-dispatcher` and `plan_eval` both consume it, `MAX_TOTAL_ACTIVE_SECS` derives from `GUEST_TOTAL_CEILING_SECS`, and a `const _: () = assert!(GUEST_TOTAL_CEILING_SECS < TURN_ZOMBIE_REAP_SECS)` fails the build if the two watchdogs ever cross again (verified: reverting the wall to 300 fails compilation with that message) |
| DEF-080 | Unbounded session history + O(all-history) lookups melted the fleet — `list_session_events(session_id, 20)` loaded and deserialized EVERY `session_event` node ever recorded (mac-jane: 106k rows / 642MB since March; vps-jane: 187k; mbp-jane: 48k / 985MB) under the single SQLite connection mutex on every last-N-events call; hotels saturated at ~100% CPU, IPC calls hit 10s timeouts, model-router config load exceeded its 55s ceiling so every philote turn failed pre-dispatch, heal-dispatcher heartbeats staled >90s into respawn-budget exhaustion (healing dead ~35h on mac-jane). Aggravators: session_turn rows up to 8.6MB carrying inline `audio_base64` TTS output that belongs in the blob store; "most recent 20" was actually ordered by node_key = random UUIDs. Live-fixed 2026-08-07 by pruning >7d history + VACUUM on all 3 hotels (927MB→87MB, 684MB→76MB, 1.1GB→35MB) | high | fixed | 3 | 2026-08-07 | codex/session-event-scan-fix — SQL-side filter/order/limit via `list_nodes_by_kind_json_eq` + expression indexes; retention sweep in aiua (`PHILOTIC_SESSION_RETENTION_DAYS`, default 7d, running turns exempt); regression tests in `session_history_tests.rs` |
| DEF-081 | Synthetic dispatches were ledgered as session turns, guaranteeing one `ZOMBIE_TURN_REPAIR` row per dispatch. `record_session_activity_from_value` opens a `session_turn` row for **every** envelope carrying a `turn_id`, but only `CompleteTask`/`FailTask` ever close one — and three dispatch kinds never send those *by design*: model-failover origin-tier probes (`probe-*`, intercepted at the top of `handle_model_response`, never a `WorkingTurn`), the LifeGraph cache sentinels `life-autorecall-prefetch`/`life-autocapture` (`datasource_response` only refreshes the session cache), and blank turn ids. The sentinels are **constant per-session ids**, so the same row reopened after every reap — one vps row cycled open→reaped daily from July 7 to Aug 24. Census 2026-08-24: **152 of 198 fleet zombie rows (77%) were probes whose "pong" had actually succeeded**. Worse than cosmetic: zombie-labeled rows feed the consecutive-zombie role-demotion counter (`is_zombie_repaired_turn`), so probe bookkeeping could demote a healthy role incarnation | high | fixed | 2 | 2026-08-24 | codex/zombie-turn-zero — `is_synthetic_turn_id()` gate at the ledger intake (session events still recorded); the sweep retires legacy synthetic rows as `SYNTHETIC_TURN_RETIRED` with no heal entry and no demotion pressure; sweep also late-closes rows whose final response already went out (`LATE_CLOSE`, status completed) instead of mislabeling served turns as zombies |
| DEF-082 | Operator messages queued behind an active turn were **silently dropped after 120s** (`evict_stale_queued_tasks` in the watchdog tick returned only a count), and the watchdog *eviction* path never drained the queue (only completion/failure paths call `drain_next_user_task`), so a message arriving during a stuck turn was doomed twice over: dropped at 120s, and even if it survived, nothing would have dispatched it after the eviction. Live evidence: three real Telegram messages to Beacon lost 2026-08-22/23 ("Are you good?" et al.) — each queued behind a turn wedged in `WaitingTool` on `life.list`, dropped at +120s with no reply, no notice, and a zombie ledger row as the only trace | high | fixed | 2 | 2026-08-24 | codex/zombie-turn-zero — stale threshold raised to `GUEST_TOTAL_CEILING_SECS + 300` (900s: strictly above the longest possible active turn, so a drop now means the drain machinery itself failed); eviction path drains the queue; a genuinely dropped task closes its ledger row (`QUEUED_MESSAGE_EXPIRED`) and sends the sender a visible notice |
| DEF-083 | Turns philote failed *cleanly* still surfaced as `ZOMBIE_TURN_REPAIR` because three `FailTask` call sites passed `session_id: None, turn_id: None` — `fail_active_turn` (every ladder-exhausted turn), approval-clear, and approval-deny — and the hotel's `FailTask` handler only closes the ledger row when the caller provides both ids (its fallback activity payload carries no `session_id`, so it no-ops on the ledger). Every "All model providers failed" turn therefore left an open row for the reaper to mislabel ~11 minutes later | medium | fixed | 1 | 2026-08-24 | codex/zombie-turn-zero — all three sites pass `Some(session_id)`/`Some(turn_id)` |
| DEF-084 | A machine-local network outage was treated as a per-provider failure, so the fallback ladder walked futilely through tiers that all ride the same dead network. mac-jane's daily crons (`architect-charter` + `lifegraph-flywheel-daily`, both 13:00 UTC) fire exactly as the Air wakes from sleep before Wi-Fi is up — Telegram polling, IPC, and every model provider fail for ~25s — producing a daily crop of `provider_error → fallback tiers → fallback_exhausted → oracle reroute → FallbackOverride → origin probes` churn (123 probe zombies on `cron:architect-charter` alone). The classifier already stamped these `network_error` / `retry_same_provider`; the dispatch loop escalated anyway after 2 quick attempts | medium | fixed | 2 | 2026-08-24 | codex/zombie-turn-zero — model-router waits out connect-class failures in place (5s waits, attempt counter not consumed, bounded by the 55s outer dispatch timeout and a 20-wait belt); a sustained outage still converts to `provider_timeout` and the ladder engages exactly as before |
| DEF-085 | `delegate.whisper` black-holed the whole turn when the specialist could not be served. Three stacked gaps: (1) the hotel's `ParacrineEmit` handler returned **success for every outcome** — role incarnation missing ("task dropped"), lookup error, hotel record missing, and even a **failed materialization it detected 1ms after parking the task**; (2) philote checked only `emit_result.is_ok()` (transport), so the hotel's refusal — had one existed — would have been discarded (DEF-068/#441's bug class on the paracrine seam); (3) a whisper past `PARACRINE_WHISPER_WAIT_SECS` (660s) was handled by evicting the ENTIRE turn with a generic "I seem to have gotten stuck" apology. Live 2026-08-25 17:29: operator asked Beacon to set up reminders → Beacon whispered to Chronos (`wait_for_response`) → Chronos guest was deactivated (`is_active=0`, and `ParacrineEmit` only seeds the record when absent, so it never reactivates) → "Role-philote [vps-jane:philote-Chronos] could not be materialized" → success returned anyway → Beacon deaf 660s → eviction apology. Diagnostic trap that hid it: hotel-core `aiua::` logs go to `PHILOTIC_LOG_DIR` files (`/opt/philotic/data/logs/` on vps), NOT journald — journald only carries guest-side targets | high | fixed | 2 | 2026-08-25 | codex/paracrine-whisper-failfast — hotel refuses honestly (`SPECIALIST_UNAVAILABLE` on undeliverable/unparkable whispers, refused parks removed so they cannot flush later); philote binds the refusal into an immediate tool_result the model reacts to (sub-second, mirrors #441); whisper deadline degrades gracefully — specialist silence becomes a `SPECIALIST_TIMEOUT` tool_result + `paracrine_whisper_timeout` heal event and the turn CONTINUES (total-active clock restarted so the CatchAll doesn't evict the recovered turn); eviction remains only as backstop if recovery itself wedges |
| DEF-086 | Two silent killers made the paracrine role plane structurally unable to work. (1) **Every hotel boot deactivated every dynamically-materialized role-philote**: `deactivate_legacy_managed_guests` treats any `{hotel}:philote-*` guest not in the static seed list as legacy — exactly the shape `ParacrineEmit` seeds at first whisper — and the `role=="agent"` arm swept the `{agent}:{role}` incarnation identity records too. Combined with `ensure_guest_active` honoring `is_active=0` as operator intent, one restart permanently killed delegation to every specialist role (Chronos had been dead since some restart; a live DB re-activation would have been wiped again at the next boot). (2) **The specialist lens never engaged**: a role-philote's fresh paracrine session auto-activated the agent's DEFAULT role (`orchestrator`) because fresh-session activation ignored `PHILOTIC_ROLE_NAME` — the whisper ran without the specialist's manifest or toolset, in the live trace answering as plain Beacon-orchestrator. Found while auditing why the 18:20 happy-path whisper produced a generic reply | high | fixed | 2 | 2026-08-25 | codex/paracrine-hardening-s1 — boot sweep exempts role-philotes (config env `PHILOTIC_ROLE_NAME`) and non-hotel-prefixed `{agent}:{role}` incarnation records while genuine legacy shapes still fall; fresh-session default role prefers the process's own `role_name` (a role incarnation exists to serve exactly that role) |
| DEF-087 | Cross-hotel responses were rejected at the WRONG hotel. The EmitTask response-route gate (RC-4) evaluated "response-like action with no concrete return guest" using **local** subscriber inference even when `target_node` was a REMOTE hotel — where the session and return guest actually live. Result: guest-less `datasource_response` / `paracrine_response` tasks bound for a peer died locally with `RESPONSE_ROUTE_UNRESOLVED` instead of being forwarded; live 2026-08-25 on vps, mac-jane's life.* datasource returns died at this gate at 15:14 and 16:24 (the mac callers saw only cache-refresh silence). Compounding: both philote response emitters (`delegate.merge`, turn-completion auto-emit) discarded the hotel's refusal (`let _ =`), so a rejected specialist answer left zero trace anywhere | high | fixed | 2 | 2026-08-25 | codex/paracrine-hardening-s2 — remote-targeted response-like tasks forward to their home hotel for resolution (local unresolved routes still reject); both emitters bind refusals as warn + heal events (`paracrine_response_rejected`, `reply_emit_rejected`) |
| DEF-088 | Telegram streaming drafts fail invisibly: `send_telegram_text` never checks the HTTP status or the `ok` field — Telegram answers every rejection (400 bad entities, 401, 429) with well-formed JSON carrying no `result`, so the parse succeeds and the function returns `None` with **zero logging**. The draft-streaming path (`upsert_streaming_draft`) has no other failure surface, so a rejected draft send means no progressive draft, ever, with nothing in the journal. Live 2026-08-26 on vps-jane (Beacon): 62 `streaming_token` deltas → 98 `partial_reply` EmitTasks → 1,388 "Partial reply observed" since 08-20 — and no draft message ever appeared, no failure ever logged | medium | fixed (pending live confirmation) | 1 | 2026-08-26 | codex/telegram-streaming-silent-send (PR #450) — sendMessage rejections logged with status + truncated body; draft creation logged for positive watched-live confirmation; test pins rejected send → `None` (next due partial retries) |
| DEF-089 | Telegram bot tokens leak into logs on transport errors: `sendMessage failed: error sending request for url (https://api.telegram.org/bot<TOKEN>/sendMessage)` — reqwest transport errors embed the full URL, and the bot token is a path segment. Observed live in mac-jane `aiua.log` 2026-08-25/26 (two distinct bot tokens in plaintext). Fix = redact the token segment before logging (or log method name only) across all membrane Telegram call sites | medium | open | 1 | 2026-08-26 | found during DEF-088 forensics; tokens rotated? — operator to decide (logs are local-disk only) |
| DEF-090 | Agents scheduled crons hours off while sounding confident: the cron schedule string is raw UTC fields, the only clock in the prompt was UTC (the fleet's `user_profile` timezone plumbing existed end-to-end but NO hotel had a correctly-keyed profile node — mac's sat under legacy key `user_profile:local-telegram`, vps had none), and nothing echoed the registered fire time back for verification. Live 2026-08-26: "ping me at 8:30pm tonight" → Beacon registered 20:30 UTC (4:30pm EDT, 4h early) while telling the operator the correct "8:30 PM Eastern / 00:30 UTC" — schedule string and prose composed independently; her three self-made timezone rules (near-duplicates from an earlier correction) changed nothing because rules are advisory prose with nothing to check against | medium | fixed | 2 | 2026-08-26 | codex/cron-timezone-truth — prompt header now renders the operator's LIVE local clock next to UTC (chrono-tz, DST-correct) when `user_profile.timezone` parses; `cron.register` echoes the actual first fire in both clocks with an instruction to verify-or-re-register before confirming; `user_profile:{hotel}` nodes seeded on mac-jane + vps-jane (America/New_York); Beacon's three duplicate rules consolidated to one that points at the header clocks and the echo |
| DEF-091 | Plan-continuation model calls never stream: a step's final `send_reply` removes the session's `ActiveTurn` entry (the RC-3 serialization point), and the continuation's model call starts with no new inbound Telegram message to re-create it — so every continuation step's `partial_reply` finds no entry, `due_snapshot` is `None`, and progressive streaming silently stops after the plan's first step. Since Beacon runs multi-step plans on most turns, most of what the operator watches is exactly the non-streaming phase. Proven live 2026-08-26 17:06 vps-jane: step 1 streamed (draft 5167), continuation finals 17:06:57 + 17:07:07 created no draft | medium | fixed (pending live confirmation) | 1 | 2026-08-26 | codex/telegram-streaming-plan-continuation — membrane re-arms the ActiveTurn (fresh heartbeat + draft state) on the `plan_continuation` turn event; the step's final removes it again as before |
| DEF-092 | Plan continuation budget only ever truncated *progressing* plans: the stall ceiling (2 consecutive empty continuations → Blocked) already kills every spinning plan, so `continuations_used >= budget` could only bind a plan that was verifiably landing steps each turn — the one kind of plan the loop exists to finish. A >8-step plan making one step per continuation was cut off mid-goal at the ceiling | medium | fixed (pending live confirmation) | 2 | 2026-08-27 | codex/philote-aggressive-plan-completion — progress refunds the per-stretch budget (a continuation that settles a new step resets `continuations_used` to 0); new never-refunded `CarryoverPlan.lifetime_continuations` capped at `PLAN_CONTINUATION_LIFETIME_CAP = 24` backstops a plan that farms continuations by growing its own step list |
| DEF-093 | Plan continuation turns could be projected the wrong tools — or ZERO tools: the synthesized brief is the continuation turn's only tool-projection relevance text, it printed step descriptions but never the bound `tool_name`s, and the keyword gates in `project_tools_for_turn` know nothing about plans. Worst case a `?` anywhere in the goal or a step description (`looks_like_conversational_goal` fires on bare `contains('?')`) classified the whole brief as conversational filler → zero tools → the continuation cannot execute a single step → 2 stalls → plan blocked without one attempt | high | fixed (pending live confirmation) | 2 | 2026-08-27 | codex/philote-aggressive-plan-completion — briefs and stop notices now name each step's bound tool (`(tool: X)`, which also fires the explicit tool-name projection match first); `project_tools_for_turn` unconditionally re-adds tools bound by the active turn's plan steps after all keyword gates |
| DEF-094 | A failed or watchdog-evicted turn stranded the plan carryover forever: `deliver_text_reply` is the only path that synthesizes continuations, and `fail_active_turn` / eviction never touch the carryover — the plan went silently dormant (resumable only by an unrelated *plan-less* turn completing, which plan-by-default makes rare). Same dormancy after checkpoint restore | high | fixed (pending live confirmation) | 2 | 2026-08-27 | codex/philote-aggressive-plan-completion — `resume_carryover_after_failed_turn` on both paths: the dead turn is charged as a stall and a continuation is synthesized; two consecutive dead turns stop the plan with a `plan_stopped` event instead of spinning |
| DEF-095 | A new plan with a different goal silently destroyed an unfinished carryover (goal-keyed replace in `plan_followup_after_turn`), and the stop notice still advertised dead affordances ("Send a message to keep going manually, or /plan drop") on paths where the carryover was already cleared | low | fixed (pending live confirmation) | 1 | 2026-08-27 | codex/philote-aggressive-plan-completion — superseded carryovers with outstanding steps emit a `plan_stopped` turn event ("superseded by a new plan"); notice text now states the truth (carryover cleared, remaining steps will not run automatically) |
| DEF-096 | `turn_status` posted orphaned status messages for untracked turns: when no `ActiveTurn` entry exists for the session (continuation turns before DEF-091's re-arm, cron sessions, any turn-less context), the ephemeral "_Running <tool>..._" upsert had nowhere to store the message id — so EVERY tool call posted a fresh italic message that could never be edited in place or deleted at final delivery, piling up as chat spam during tool loops. Also entirely unlogged (both branches silent). Observed by the operator live 2026-08-26 as "tool call messages" accumulating during Beacon's delegation tool grind | low | fixed | 1 | 2026-08-27 | codex/telegram-turn-status-hygiene — status line renders only for tracked turns (skip + INFO log otherwise); upserts now logged with message_id |
| DEF-097 | LifeGraph review patches impersonated executable skill/schema changes. Beacon proposed `patch_music_skills_registration_v1` and `patch_music_repertoire_schema_v2`, received operator approval, then `life.patch.apply` returned only `not_applicable`; she inferred the runner lacked SkillPatch/SchemaPatch handlers. Live records proved both were `status=proposed` prose-only payloads: the SkillPatch contained no skill definitions, and the SchemaPatch contained no `ontology_extension` (the model-facing schema did not expose that field). Compounding: a short approval reply could drop the on-demand `skill.authoring` surface, hiding the real `skill.register` + `skill.assign` actuator | medium | fixed (pending live confirmation) | 2 | 2026-08-27 | codex/beacon-skill-actuator — model-facing contracts name the authority split and expose executable ontology extensions; `not_applicable` returns patch kind/reason/next actuator; build/apply language plus approval continuations retain skill administration tools |
| DEF-098 | Model-invoked `life.observe` results were hijacked by the direct-command short-circuit: `handle_tool_result` matched ANY pending life.observe whose arguments carried `evidence.claim_summary` (i.e. every well-formed call, not just the operator's "record this …" text-command lane), delivered the canned "Recorded this X in LifeGraph" receipt, and force-completed the turn. One observation per turn, structurally: the model never re-entered to do items 2..N, no plan was ever evaluated (zero plan_eval events), and DEF-092..095's carryover machinery never engaged because turns never carried plans. Live 2026-08-27 20:19–20:22: operator listed five habits, Beacon recorded exactly one per message, operator asked six times ("why aren't you add them ALL", "add them", "add them all") | high | fixed (pending live confirmation) | 2 | 2026-08-28 | codex/philote-life-observe-reentry — the short-circuit now requires `is_direct_life_observe_origin` (route marker stamped only by the text-command parser) or a contract error (bounded model retry preserved); model-invoked successes and transport failures flow through normal loop re-entry so the model finishes the list and writes its own reply |
| DEF-099 | `should_plan` missed short enumerations: "more habits that i am building: duolingo morning and evening, morning supplements and evening, reading before bed" has no request-marker word, no `?`, one sentence, 17 words — no plan directive was injected for a five-artifact message (the exact shape the plan machinery exists to protect) | medium | fixed (pending live confirmation) | 1 | 2026-08-28 | codex/philote-life-observe-reentry — two or more `, `/`; `/` and `/` & ` separators now classify the message as work (same threshold as `description_enumerates_artifacts`) |
| DEF-100 | Checkpoint restore silently reset every watchdog wait-clock to "now". `turn_waiting_since`, `parked_approval_since`, and `parked_plan_since` are `Option<Instant>` on `SessionState`, but `from_checkpoint()` set all three to `None` unconditionally — `Instant` has no wall-clock epoch and was never persisted. The watchdog's reconcile loop treats a `None` stamp as "first seen this tick" and stamps `now`, so any session that got unloaded and reloaded (idle eviction, guest restart) mid-wait had its deadline silently restart from zero, extending the true wait by however long the reload took plus a full fresh grace window — and the eventual eviction's logged `elapsed_secs` under-reported the real wait by that same amount, hiding the severity from anyone reading logs. Live 2026-08-29: architect-charter's `skill.register` approval request sat unanswered for 84 minutes (one `approval_requested` session_event at 13:02:08, one eviction at 14:26:38) while the watchdog logged `elapsed_secs=304` — a ~16x under-report. A same-day sibling incident (agent-bjork-01, 2026-08-27 evening) showed the adjacent UX problem: a 4-step `skill.register` plan re-requests live approval on EACH step with no distinguishing marker, so an operator who approved step 1 can miss step 2's identical-looking prompt — not this defect, but the same session repeatedly hit the 300s deadline for a related reason and is worth a follow-up (batch or number multi-step approval prompts) | high | fixed | 2 | 2026-08-29 | codex/philote-checkpoint-clock-loss — `checkpoint_json()` persists each stamp as a wall-clock unix-seconds sibling key (`turn_waiting_since_unix`, `parked_approval_since_unix`, `parked_plan_since_unix`); `from_checkpoint()` reconstructs an approximate `Instant` from the elapsed wall-clock delta, gated on the corresponding turn/park state actually surviving restore. `active_turn_since` intentionally left as-is — its `None` path already degrades to tick-local `now` by documented design, unlike the other three which the reconcile loop treats as authoritative |
| DEF-101 | An approval park loop had no total-age ceiling, so a turn could live for hours and every artifact of the outage understated it ~35x. A parked turn answered to exactly one deadline and every approval round-trip re-armed it: `park_active_turn_for_approval` re-stamps `parked_approval_since`, `restore_parked_approval_turn` re-stamps `active_turn_since`. A multi-step plan that re-requests approval per step (the follow-up DEF-100 explicitly flagged) therefore reset BOTH clocks on every park -> approve -> park cycle, and nothing measured the turn end to end. The hotel's 660s reaper could not cover it either — it only sees turns it still counts as `running`, so a parked turn is invisible to it. Live 2026-08-30 mac-jane: an `agent-bjork-01` turn ran 12:46:34 -> 15:45:22 (**2h58m48s**) and was evicted reporting `elapsed_secs=302`, the age of its last park alone; the operator saw a philote that had simply gone silent. Sibling of DEF-100, not a regression of it — that fix covered checkpoint restore, this is the park/resume round-trip which never touches a checkpoint | high | fixed | 2 | 2026-08-30 | codex/parked-turn-total-age-ceiling (PR #475) — `SessionState::active_turn_started_unix` stamped once at `start_turn` and never re-stamped, persisted through the checkpoint; `turn_budget::TURN_TOTAL_AGE_CEILING_SECS` (1800s) held ABOVE `TURN_ZOMBIE_REAP_SECS` by const assert since the guest must bound what the reaper cannot see; both parked branches evict on total age and the eviction label now carries the true `turn_age`. Park-loop test verified to fail without the ceiling |
| DEF-102 | The dialogue window was the one unbudgeted context layer, and it was injected two to three times per request. The rolling window bounds turn COUNT (`memory_window_size`) and turn AGE but never turn SIZE, so one pasted document entered the window and was re-sent on every request until it aged out. Separately `project_knowledge` emitted `[Recent session context]` (the whole window) followed by `[Recent summary]` (its last three turns) — a verbatim second copy in the same layer — while the Session layer added its own recap, so the newest turns shipped three times. `persona`, `recalled_memory` and `rules` were already budgeted; the dialogue was not. Live 2026-08-30 mac-jane: `agent-coach` requests reached **236,217 bytes**; Gemini 503'd under load, the rescue leg to OpenRouter blew the router's 55s dispatch budget, and the turn died `MODEL_EMPTY_RESPONSE` twice within the hour. Note the payload is NOT why Gemini 503'd (capacity; an oversized request 400s) but is squarely implicated in the leg with a time budget | high | fixed | 2 | 2026-08-30 | codex/parked-turn-total-age-ceiling (PR #475) — `InjectionBudget::dialogue_chars` (8000); `render_dialogue_window` admits newest-first, renders oldest-first, names what it dropped, and always keeps the turn being answered; `[Recent summary]` removed from the knowledge layer. Elided text stays in the `session_turn` ledger, and anything that must outlive the window belongs in Muninn |
| DEF-103 | `skill.register` treated every registration as maximally risky, always demanding a fresh live Telegram approval with no risk gradient — a narrow tracking skill wrapping only tools the registrant already holds got the identical unconditional interrupt as one that could grant `bash.exec`, and "Trust for session" is explicitly immune to the unconditional gate so it never helped. Compounding it, a `WaitingApproval(parked)` eviction discarded the evicted turn's `active_plan` with no carryover set — the one eviction path the existing plan-carryover machinery (DEF-092..095) didn't cover — so the operator's next message restarted the whole goal from step 1 instead of resuming at the step that actually timed out. Live 2026-08-27: a 4-step `skill.register` plan (narrow music-practice tracking skills, each already covered by the registrant's own bindings) hit three separate approval-timeout evictions, restarted from scratch each time, and never registered a single skill | high | fixed | 3 | 2026-08-31 | codex/skill-governance-hardening — `skill.register` downgrades from the unconditional gate to normal policy-governed approval when its declared `allowed_tools`/`allowed_classes` are already a subset of the registering session's own bindings (any `allowed_skills` DAG edge stays conservative-unconditional — philote cannot resolve it locally); approval cards name how many other plan steps will hit the same gate so a repeat reads as a different step, not a duplicate already answered; an approval-timeout eviction now stashes the evicted turn's plan into `carryover_plan` before clearing state, so the pre-existing (previously starved) `resume_carryover_after_failed_turn` call actually has something to resume; the unblock notice distinguishes an approval timeout ("resumes" wording) from a generic hang. See SKILL_GOVERNANCE_HARDENING_PROPOSAL.md |
| DEF-104 | The hotel's blob HTTP listener bound `0.0.0.0` while declaring itself `Local` to the perimeter, exposing an unauthenticated write endpoint. `main.rs` declared the blob listener to `HotelPerimeterService` as `ListenerDecl { purpose: "blob", bind_addr: Ipv4Addr::LOCALHOST }` — and the adjacent comment said "Blob binds to 127.0.0.1 (Local)" — but the actual serve call hardcoded `format!("0.0.0.0:{}", blob_port)`. The two sites were ~700 lines apart and nothing tied them together. Because the hotel derives and persists its exposure tier (`__hotel_perimeter__`) from the DECLARATION, the snapshot reported the blob plane as `Local` while the real socket was world-reachable — the perimeter record was a lie rather than merely incomplete. The blob plane has no authentication of any kind: `POST /upload` accepts anonymous 100MB multipart writes and `GET /download` is a bare `ServeDir` (`crates/aiua/src/service/blob.rs`, 108 lines). Verified live 2026-08-31 from off-tailnet: `31.97.130.98:16467` (blob) and `:16468` (exec) both reachable from the public internet on vps-jane, whose `iptables` INPUT policy is ACCEPT with ufw not installed. Net effect on any host with a public IP: an anonymous internet-facing 100MB upload endpoint, and per DEF-078 a full disk wedges the hotel SILENTLY. Downloads are content-addressed (`sha256-<hex>`) so read requires knowing the hash, and the exec plane correctly drops unsigned frames — upload is the exposed vector. Note the mesh AUTHENTICATES but does not ENCRYPT (`authz.rs` is `Hmac<Sha256>` only), so Tailscale was supplying all wire confidentiality; this is why a public-path transport fallback cannot be added before wire encryption | high | fixed | 1 | 2026-08-31 | codex/blob-perimeter-bind — single `BLOB_BIND_ADDR: Ipv4Addr = LOCALHOST` const now feeds BOTH the `ListenerDecl` and the serve call so they cannot drift again; regression test `blob_listener_binds_loopback_to_match_perimeter_declaration` asserts loopback with a comment stating that widening requires adding auth first. Safe because every real consumer already reaches blobs over loopback — `blob_base_url` is hardcoded `http://127.0.0.1:{blob_port}` (main.rs) and shipped to guests as `PHILOTIC_BLOB_BASE_URL` (verified live: `http://127.0.0.1:16371`); `EventPayload::BlobRef.source_hotel_ip` exists in the protocol but has NO fetch-site consumer, so no cross-hotel blob transfer is implemented to break. A future cross-hotel blob fetch must add authentication and bind to the mesh interface — not revert to `0.0.0.0`. Runtime mitigation on already-deployed hosts (restrict 16466-16468 to `100.64.0.0/10`) is separate and operator-owned |
| DEF-105 | `require_skill_admin` (aiua `ipc.rs`) accepted only the literal identity roles `orchestrator` / `management`, but a materialized role-incarnation philote registers with its routing key as the role (`role:{agent_id}:orchestrator`, see `RoleIncarnationRecord::routing_role` and `philote/src/main.rs`). So no real incarnation had ever passed the gate: every registration in the audit trail came from drill/UAT guests whose identity role was literally `orchestrator`. Live 2026-09-04 18:07 mac-jane: the Self-Improvement Loop's distill whisper routed to `agent-bjork-01:orchestrator`, the model made a well-formed `skill.register` (`music.weekly-practice-review`), and the hotel answered `Rejected skill administration from unauthorized role guest_id=agent-bjork-01:orchestrator role=role:agent-bjork-01:orchestrator`; the distiller then replied `DISTILL: nothing`. Same gate guards `skill.assign` / `skill.set_state` / `skill.audit` | high | fixed | 1 | 2026-09-04 | codex/self-improvement-distill-routing — `skill_admin_role()` strips the `role:{agent}:` prefix before comparing, so scoped orchestrator/management incarnations pass and every other scoped role still fails; unit test covers both forms |
| DEF-109 | Every MCP `tools/call` aimed at a philote became a full model turn, and the one deterministic bypass (`context.capture`, `turn_loop.rs`) parsed only the legacy `{"tool","args"}` content shape — a config-driven endpoint (`mcp.provision`) sends `{"action","payload",...}`, so a provisioned `context.capture` tool would have captured an empty payload. There was no way for a provisioning philote to declare "answer this deterministically" for a philote-targeted tool, no schema enforcement on inbound args, and no error path: the philote's `emit_error_reply` sends `send_reply` text, so validation failures could only ever surface as *successful* results | medium | fixed | 3 | 2026-09-05 | codex/mcp-endpoint-steward — `McpToolSpec.handler` (`McpHandlerPolicy`: `validate_input`, ordered `static`/`reflex` steps, `model`/`error` fallback) forwarded by membrane-mcp in `raw_transport`; philote `mcp_ingress` (parse both shapes, schema validation, `${payload.x}` templating) + `mcp_handling` ladder before `handle_user_message`; new `send_error` reply mapped to `OutboundReply::Error` (→ `isError:true`); `context.capture` reads args via the shared parser; policies validated at provision time on both philote and hotel; `mcp.endpoint_steward` skill seeded + SKILL.md |
| DEF-106 | `seed_orchestrator_roles` (aiua `main.rs`) upserts every agent's `orchestrator` role incarnation on EVERY hotel boot with `home_node: None` hardcoded ("the hotel seed is canonical"), preserving only `turn_loop_config`, `content_policy`, `role_identity_addendum`. A runtime `role.set_home` that moved the orchestrator to another hotel is therefore silently reverted by the origin hotel's next restart — the agent is un-migrated with no log line saying so. `preserve_runtime_guest_activation` already guards `is_active`/`active_pid` for guests; nothing guards placement fields. Found 2026-09-05 auditing the Relocation Ceremony (G6); violates `graph-truth-outlives-config-seed` | high | fixed | 1 | 2026-09-05 | codex/relocation-r1 (RELOCATION_CEREMONY_PROPOSAL R1) — `seed_orchestrator_roles` carries the existing `home_node` + `placement_updated_unix` forward instead of hardcoding `None`; `role.configure` preserves the stamp too; regression test `seed_orchestrator_roles_preserves_runtime_home_node_across_reseed` |
| DEF-107 | `transport.set_home` writes the `membrane_transport_home` record only to the graph of the hotel that handled the IPC call, and the record is in no gossip payload (`HotelStateSyncPayload` carries guests/agents/model_profiles only). The target hotel's poller checks its OWN local record, so after a move the two hotels disagree about who owns the token until someone writes the record on the target too. The old poller learns it lost authority only when its next lease renewal returns `LEASE_TRANSPORT_HOME_MISMATCH`, then re-probes every 180 s — no push, no drain, so a "seamless" move has a multi-minute deaf window and can drop an in-flight turn. Found 2026-09-05 auditing the Relocation Ceremony (G1, G7) | medium | fixed | 2 | 2026-09-05 | R2 (codex/relocation-r2) closes the stand-down half: hotel pushes `IpcResponse::TransportHomeChanged` (with `hotel_is_home`) to every local guest on a local `transport.set_home` AND on a gossiped record applied by `placement_sync` (beacon → `placement_change_tx` → broadcast); the Telegram seat gains an explicit `Standby` stand-down (entered on `LEASE_TRANSPORT_HOME_MISMATCH` or a home-moved push), stops its poll loop at once, releases the lease on the next tick, and a home-moved-here push arms an immediate re-probe on the next 20 s lease tick. R1 (codex/relocation-r1) closed the replication half: both home record kinds carry a placement stamp, runtime-stamped role homes + transport homes ride `HotelStateSync`, peers apply last-writer-wins (`ansible_mesh_core::placement_sync`), and `RoleHomeSet`/`TransportHomeSet` mark hotel state dirty so the change gossips immediately. Still open: push stand-down + standby-before-acting (R2) |
| DEF-108 | Two different `MeshCatalogSyncPayload` structs share one name: `membership.rs:88` (abstract_tools / abstract_skills / toolset_profiles — the canonical catalog) and `heartbeat.rs:43` (`peers` directory). The only emitter (`heartbeat.rs:302`, on reconnect) and the `MsgType::MeshCatalogSync` handler in `beacon.rs:348` both use the `heartbeat.rs` one; the `membership.rs` payload is constructed only inside its own test module. So the canonical mesh catalog sync that `docs/task.md` marks done ("replicate `abstract_tool`, `abstract_skill`, and `toolset_profile` records on change and periodic full sync") never crosses the wire — a hotel that never receives a role handoff for an agent never learns its toolset profile. Code-confirmed 2026-09-05 auditing the Relocation Ceremony (G10); live confirmation pending | medium | open | 2 | 2026-09-05 | — (RELOCATION_CEREMONY_PROPOSAL R8) |
| DEF-110 | A philote-targeted MCP `tools/call` fanned out to EVERY agent philote on the hotel. `membrane::runtime::build_inbound_request` only carried `target_guest_id` on the cross-node `EmitTask` path; a local philote target became `CreateTask { target_role: "agent" }`, which the hotel delivers to every `agent` inbox subscriber. Compounding it, an unscoped agent id (`agent-bjork-01`) never matches a materialized incarnation (`agent-bjork-01:orchestrator`), so even the pinned path would have matched nobody. Live 2026-09-05 16:45 mac-jane, first smoke of the steward ladder: one `ping` call produced 8 static answers, one `strict` call 8 `isError` replies, membrane-mcp logged `deliver: no pending receiver` for the 7 duplicates, and the `recall_note` call's model fallback started in 2 philotes while the winning reply was astrid's (a different agent's) Muninn 401. Every `mcp.provision` with `{kind:"philote"}` since the gateway landed had this shape; `context.capture` on vps-jane runs on a single-agent hotel so it never showed | high | fixed | 2 | 2026-09-05 | codex/mcp-endpoint-steward — membrane pins `target_kind=philote` + hinted guest id to the local node via `EmitTask` (`philote_target_without_node_is_pinned_to_local_guest_via_emit_task`; tool targets stay role dispatch); hotel `select_guest_targets` resolves an unscoped agent id to exactly one live incarnation (orchestrator, else lexically first) and never broadcasts (`select_guest_targets_never_fans_out`) |
| DEF-111 | A governed HTTP request placed at a remote exit hotel was black-holed. 2026-09-05 19:52 mac-jane: coach's `http:hevy-webhook-api.request` (binding placement `prefer_hotel: vps-jane`, fallback `local_with_audit`) was logged `EmitTask mapped to TaskInvoke for vps-jane-aiua-01/egress-http-runner guest=None` and the ledger cursor advanced, but vps-jane's hotel journal shows no receipt and no egress activity in the window (mac→vps datasource tasks in the same minutes WERE received), mac-jane's own egress runner was dormant (it only registered at the 20:18 restart), so nothing ever answered; the philote sat `WaitingTool` for 300 s and was evicted. Placement resolution reported the exit hotel reachable, so the `local_with_audit` fallback never engaged. Coach then told the operator it was Tailscale, then a Muninn token. Mitigation applied live: binding re-registered with `placement: local`. Root cause of the vps non-delivery (ledger relay vs. TaskInvoke for a non-agent role from a foreign node) still open | medium | open | 2 | 2026-09-06 | — (mitigated: `hevy-webhook-api` placement local; `integration.steward` skill tells agents to prefer local placement and to read a hang as runner/placement, not network) |
| DEF-112 | At least `role.set_home`'s and `transport.set_home`'s philote tool handlers (`tool_exec.rs`) match a hotel rejection as `Ok(IpcResponse::Error(msg))`, but the hotel-side gate rejects via the `IpcResponse::error()` helper, which constructs `Standard { ok: false, message, .. }` — a structurally different variant of the same untagged enum. `IpcResponse::Error(String)` is a real, separately-used variant (constructed directly by e.g. the cron IPC handlers), so the pattern is not dead code in general, just wrong for these specific callers. Effect: a non-admin caller's `role.set_home`/`transport.set_home` rejection falls through to the `Ok(_) => unexpected hotel response` arm, so the operator/philote sees a generic `UNEXPECTED_RESPONSE` instead of the real authority-denial message. Found 2026-09-08 building R3's `hotel.materialize_request`/`hotel.materialize_status` tools, which were modeled on `role.set_home`'s handler and initially copied the same bug (fixed in those two before merge — they now match both variants). ~20 other `Ok(IpcResponse::Error(msg))` sites in `tool_exec.rs` were not individually audited; some may legitimately expect the `Error` variant from handlers that construct it directly | low | open | 1 | 2026-09-08 | — (workaround in `hotel.materialize_request`/`hotel.materialize_status`: match both `IpcResponse::Error(msg)` and `IpcResponse::Standard { ok: false, message: msg, .. }`) |
| DEF-113 | The plan-eval atomicity heuristic (`plan_eval::description_enumerates_artifacts`) flagged any step description with two commas as "bundling several outcomes", and a non-atomic step is never settled whatever its evidence. Live 2026-09-11 11:00 UTC Beacon morning brief: `Log Jared's drive with Daxton to school on Thursday morning, 2026-09-10, as a proposed event` and `Retrieve oldest active open loops, aspirations, and roles` (a single `life.recall`) were both flagged, stayed outstanding through 3 continuations, the model re-ran the same two `life.observe` calls each turn (duplicate LifeGraph events 584/585 of 580/562), FOUR Telegram messages went out for one cron brief, and the last reply said "All steps … fully executed and verified" over a `blocked` verdict (stalls=2) | high | fixed | 1 | 2026-09-11 | codex/philote-say-do-continuity — a list needs a conjunction (`A, B and C` / `A and B and C`) or ≥2 lines, not bare commas; steps bound to read-only tools (`.recall`, `.list`, `.search`, `.get`, `.status`, `.feedback`, …) are never bundles; a plan status trailer (`⏳ n/m steps verified…` / `⚠️ Plan stopped before finishing…`) rides every continuation/stop reply so the operator sees the evaluator's verdict, not only the model's claim |
| DEF-114 | A text-only reply that announces execution ends the turn with nothing executed. `interpret_model_output` turns any plain text into a turn-ending `Respond` with no check that the promised work happened; the iteration-cap wrap-up prompt only said "Provide your final response". Live 2026-09-11 13:26 UTC: the operator sent 5 open-loop updates, Beacon spent its 11-call budget on `life.recall` + 9 raw `graph.query` (4 Cypher errors), hit the cap, and replied "Here is our immediate true-up battle plan: 1–4 … Executing Step 1 now." — zero writes; operator: "Did you execute that?"; Beacon: "No … the system handoff stopped" (confabulated) | high | fixed | 2 | 2026-09-11 | codex/philote-say-do-continuity — say-do gate in the `Respond` arm: a promise-phrased reply (`reply_promises_unexecuted_action`) from a turn with no tool call is re-entered ONCE with tools intact and a `[Say-do check]` hint; at the cap or after the nudge it is delivered with an explicit "Nothing described above as executing has actually run yet" trailer; the cap wrap-up prompt now demands an account of done/not-done and an `active_plan` for the remainder so the continuation loop carries it |
| DEF-115 | Replayed dialogue carried no per-turn timestamps and the internal `[Plan continuation n/N]` briefs were replayed verbatim. The `[System]` header clock is fresh every call, but `render_dialogue_window` rendered `User:/Assistant:` pairs with no date, so the evening turn's "tomorrow" read as tomorrow again the next morning — live 2026-09-11 07:00 EDT, Beacon's brief headed "Friday, September 11, 2026" announced the Icebreaker "scheduled for tomorrow (Friday, September 11)". At 13:26 UTC the 8000-char dialogue budget held three copies of the continuation brief and "[7 earlier turn(s) elided]" — the operator's own morning conversation was gone from context, and `Recent summary` was the internal brief text | high | fixed | 1 | 2026-09-11 | codex/philote-say-do-continuity — every replayed turn is stamped with its own operator-local clock (`[2026-09-10 22:00 EDT] User: …`, UTC fallback) plus a legend that relative words are relative to that stamp; the structured `dialogue_window` entries carry `at`; continuation briefs collapse to their headline in the window and in `Recent summary` (full text stays in the hotel `session_turn` ledger) |
| DEF-116 | `life.recall` returned a well-formed, `ok`, EMPTY packet for every explicit call. A bare `{query_id, query_text}` call has no `named_strategy`/`operator_intent`, so `NamedRecallStrategy::from_task` defaults to `SemanticPivot`, whose dispatch loop iterates `semantic_pivots` (empty → zero queries) and whose recency fallback labels derive from the same empty list. The auto-embedded `query_text` vector was computed and discarded. Live 2026-09-11: all 6 explicit `life.recall` calls in Beacon's session returned `ranked_packets: []` (token_budget 1200 = the 6-packet default), so the model could not see existing nodes, re-observed facts already in the graph, and fell back to raw Cypher to find the MRI commitment | high | fixed | 1 | 2026-09-11 | codex/philote-say-do-continuity — a pivot-less `SemanticPivot` dispatches as `current_prompt_semantic` (core-space sweep with the embedded query_text; logged) |
| DEF-117 | `life.commit` MERGEd on `{id: $id}`, so committing an unknown id manufactured a brand-new node marked `confirmed` and reported "committed". Live 2026-09-11 13:28 UTC: Beacon committed `claim_ref.id = "557"` (Memgraph's internal id copied from a `graph.query` row) → stray `Commitment {id: "557"}` (internal 590) confirmed, the real `life:commitment:mei_due_date_update_20260909` (557) left `proposed`, and the reply claimed the true-up was "promoted to confirmed truth". Same turn: `MATCH (n:OpenLoop {id: 570}) SET …` matched nothing (ids are strings) and was also reported done | medium | fixed | 1 | 2026-09-11 | codex/philote-say-do-continuity — commit is MATCH-only (`n.id = $id OR id(n) = $id_numeric` for an all-digit id); no row → provider error "life.commit target not found … life.recall/life.list first"; live graph cleanup APPLIED by the operator 2026-09-11 (`scripts/lifegraph-patches/2026-09-11-beacon-trueup.cypher`): stray 590 deleted, 557 confirmed, duplicate events 584/585 retired, loops 531/532 resolved with `RESOLVES` edges from the reinstatement event, and five OLDER strays from the same bug (ids "66","108","33","11","12") found and removed — the "66" stray held the only copy of the haircut/dry-cleaners resolution, now recovered onto the real loop; `strays_remaining = 0` |
| DEF-118 | A lived-fact report that resolves a recalled open loop was projected ZERO tools, and the model claimed the write anyway. `project_tools_for_turn_by_relevance`'s conversational gate (`looks_like_conversational_goal` + no skill keyword) returns an empty tool list on purpose so filler stays tool-free, and it deliberately ignores the injected-LifeGraph-context signal. Live 2026-09-11 18:23 UTC, first message after the DEF-113..117 deploy: "I gave my icebreaker speech in toastmasters today! And i did ok. Nadi came in support." tripped the gate on "ok"; auto-recall had injected the very loop it resolves; the request carried `[Plan first]` and `tools_for_model: []`; Beacon replied "I have logged this historic event on your LifeGraph" — nothing was written (loop still `proposed`, no event, nothing mentions Nadi). The DEF-114 say-do gate did not fire: it was scoped to present-tense promises, and this is a past-tense claim | high | fixed | 1 | 2026-09-11 | codex/philote-lived-fact-tools — the conversational gate yields when the message shares ≥2 distinctive words with a recalled `life-graph` record (`turn_reports_on_recalled_life_context`; gratitude/filler still tool-free); the say-do gate also catches past-tense write claims from a tool-less turn (`reply_claims_unbacked_write`: "I have logged/recorded/updated/…", "has been resolved", …) and only re-enters when the turn would actually get tools, else delivers with a correction trailer |
| DEF-119 | "Plan, run it, evaluate" was prompt advice, not a runtime contract. The `[Plan first]` directive was injected and ignorable; tools were projected from message keywords before any plan existed, so a plan could not bind tools it was never given; `plan_eval` only runs when a plan was declared and only after the reply is sent; and a text-only reply ended the turn with nothing to continue. Operator question 2026-09-11 after the Icebreaker turn: "why isn't she creating a plan, running it, and then evaluating that it was complete?" — because nothing made her | high | fixed | 3 | 2026-09-11 | codex/philote-plan-contract — (1) **outcome reflex**: a past-tense outcome report (`reports_an_outcome`) that names a recalled open loop/commitment/next action seeds a harness-owned two-step plan (`life.observe` the outcome as a confirmed Event → `life.commit` the exact node id with `loop_status: resolved`) BEFORE tool projection, rendered as `[Outcome reflex]` in the working projection, `plan_seeded` turn event; plan-bound tools always project and `plan_eval` verifies the steps; (2) **plan-worthy statements keep their tools**: request markers, enumerations, and outcome reports bypass the conversational zero-tools gate (`is_plan_worthy_statement`, deliberately narrower than `should_plan` so a two-sentence thank-you stays tool-free); (3) **plan gate**: a plan-worthy statement answered with plain text — no plan, no tool call — is re-entered once with `[Plan gate]` ("declare and execute, or say plainly no action is needed and why"), `plan_gate` turn event; questions are exempt. **Follow-up 20:46 UTC** (first live turn on this build): the gate fired (22 tools, one `[Plan gate]` re-entry) but nothing was seeded because the LifeGraph prefetch cache was stale after a 2h23m quiet spell (max age 30 min → zero records injected → no loop to bind), and the second reply was "we already successfully initialized and processed the … event" — a false claim the detector missed (adverbs between subject and verb, "we" subject). Fixed same branch: the reflex now seeds a recall-first three-step plan (`life.recall` → `life.observe` → `life.commit` resolving what step 1 found, else confirming the outcome Event; "never invent an id") when no loop is in context; the claim detector strips adverbs and covers "we"/"was"/"were" subjects and executed/processed/initialized/applied verbs |
| DEF-120 | A false write claim became memory. The 18:23 UTC turn's reply "I have logged this historic event" (zero tool calls) carried a `memory_candidate`; the Attend hook stored it in Muninn as `toastmasters-icebreaker-delivered`, and the 20:46 UTC turn recalled it as evidence that the work was already done — "no further database changes are needed". The recalled-memory precedence note ("PAST state … trust the turn") did not stop the model from treating its own earlier claim as fact | high | fixed | 1 | 2026-09-11 | codex/philote-outcome-reflex-v2 — `deliver_text_reply` drops both the LifeGraph auto-capture and the Attend-hook `memory_candidate` when the turn made no tool call and the reply claims or promises a write (`reply_claims_unbacked_write` / `reply_promises_unexecuted_action`), with a warn log; the already-stored engram must be forgotten in Beacon's vault by hand |
| DEF-121 | A genuinely bundled step ("Log key contacts: Nadi, Daxton, Gabby, and Brandon as Person nodes") is correctly flagged non-atomic and can never settle — but the model never splits it; it re-runs all four observes on every continuation instead. Watched-live 2026-09-12 11:00 UTC on the DEF-113 build: all four Person nodes were written in turn 2, the step stayed outstanding, and the brief still went out as FOUR Telegram messages to a `blocked` verdict (stalls=2). The per-turn stamps and the status trailer worked; the loop did not | high | fixed | 1 | 2026-09-12 | codex/philote-plan-autosplit — the harness splits for the model: `set_active_plan` normalizes every incoming plan via `plan_eval::split_bundled_steps` (one step per enumerated item, same tool, lead-in kept, trailing clause trimmed, imperative verb lifted, fresh ids so nothing inherits "done"), and `verify_plan_steps`' distinctive-token pass credits each observe to its own item; regression test replays the live step and tool history to a `complete` verdict |
| DEF-122 | `life.commit` rejected every model-authored call that omitted `packet_id` ("contract validation failed: packet_id must not be empty") while `life.observe` already defaults it. Live 2026-09-12 16:59, 17:00, 17:01 UTC: every first commit attempt failed and cost a retry model call before succeeding | low | fixed | 1 | 2026-09-12 | codex/lifegraph-observe-update-commit-packet — `LifeCommitInput::normalize_defaults` fills `commit-<ulid>`; the provider calls it after parse, before plan |
| DEF-123 | Re-observing an existing node silently kept its old `claim_summary`: `compile_observe`'s ON MATCH branch updated confidence/packet/dates but never the summary or `observed_at`. Live 2026-09-12 16:49 UTC: "My home address is 137 Harmony Grove Rd" re-observed `life:place:home` (created a minute earlier with a generic summary); the runner answered `observed`, Beacon told the operator the address was recorded, and the node still reads "Home is established as a structured Place node…" (Office, being new, got its address) | medium | fixed | 1 | 2026-09-12 | codex/lifegraph-observe-update-commit-packet — ON MATCH takes the new summary and `observed_at` when the node is not `confirmed`/`retired` (confirmed truth is only rewritten by `life.commit`), always stores `last_observed_summary`, and returns `summary_updated` so the caller can see whether the text landed; the Home address must be re-observed once after deploy |
| DEF-124 | `role.set_home`/`transport.set_home`/`hotel.materialize_request`'s `target_hotel` argument is documented with a bare `hotel_name` example (`role.set_home`: "e.g. 'mac-jane'"), but every routing comparison that actually consumes the stored value (`home_node != local_node_id`, an `EventEnvelope.target_node_id` mesh-address lookup) is keyed on the full mesh `node_id` from `hotels.capabilities.node_id` (e.g. `"vps-jane-aiua-01"`, an operator-chosen deploy-time value distinct from `hotel_name`). Passing the tool's own documented example silently fails to route anywhere — no error, no delivery — because the receiving hotel never gets the mesh envelope. Found live 2026-09-09 rehearsing the R3 watched-live gate: `hotel.materialize_request` with `target_hotel: "vps-jane"` returned success locally but vps-jane's graph and process list showed nothing; retrying with the real node_id `"vps-jane-aiua-01"` worked end to end. `role.set_home`'s only prior watched-live proof (R1) only checked that `home_node` survives a hotel restart, never the actual cross-hotel comparison against `local_node_id` — so this exact gap was never previously exercised despite `role.set_home` being the Relocation Ceremony's core SWITCH mechanism | high | fixed | 2 | 2026-09-09 | codex/relocation-node-id-resolve — new `IpcServer::resolve_hotel_node_id` accepts either a bare `hotel_name` or an already-canonical `node_id` and resolves to the real `node_id` via `graph.list_hotels()`; wired into `handle_set_role_home`, `SetTransportHome` (including every `standby_hotels` entry), and `handle_materialize_request`; an unresolvable reference is now a loud rejection (`*_UNKNOWN_HOTEL`) instead of a silent non-delivery. Regression tests added for all three call sites (none of them — including `set_role_home` — had unit coverage before this) |
| DEF-125 | A turn WITH tool calls could still over-claim: the say-do/claim gates only fire on a turn with no tool call, and `plan_eval` only checks steps the model declared. Live 2026-09-12 11:00:27 UTC: a continuation turn whose only writes were four Person observes replied "Successfully created the proposed Trip node (`life:trip:utah_20260928_20261004`)" — the trip landed two turns later; 16:47 UTC: two people observed, an "encounter event" and a Mint Coffeehouse note claimed with no write | medium | fixed (ids only) | 1 | 2026-09-14 | codex/philote-claim-audit — `deliver_text_reply` audits every `life:<label>:<slug>` id the reply cites (`cited_life_ids`) against this turn: backed if a successful write call (`life.observe/.batch/.commit/.resolve/.patch.*`, `graph.mutate/.query`) named it in its arguments or result, or if it was already known (recalled record, read-tool result, the operator's message); anything else gets a correction trailer naming the ids and a `claim_audit` turn event. Claims without an id (the "encounter event") are still not caught — that needs the LifeGraph steward skill's integrity pass, not the reply gate |
| DEF-126 | `subagent.spawn` (and `transport.set_home`, `role.set_home`, `hotel.materialize_request`) refused every role-incarnation philote: the IPC gate compared the registered role string to `"agent"`, but incarnations register as `role:{agent_id}:{role_name}` (the routing role aiua itself injects via `PHILOTIC_ROLE_INBOX`). Live 2026-09-14 18:43 UTC, mac-jane: bjork's orchestrator incarnation registered `music.repertoire-gardener`, called `subagent.spawn` with it and got `SUBAGENT_FORBIDDEN`, then did the delegated LifeGraph write inline and told the operator the harness had been used | high | fixed | 1 | 2026-09-14 | codex/bjork-spawn-gate — all four gates now use `is_agent_handoff_caller` (base `agent` OR a role-incarnation record whose routing role matches); IPC tests: incarnation identity spawns, `tool` identity still refused |
| DEF-127 | A turn WITH tool calls could present a FAILED step as done: the say-do gate returns early once any tool has run, and the claim audit only checks cited `life:` ids. Live 2026-09-14 18:43 UTC (same turn as DEF-126): "Using that safety-harness, I have gone ahead and swept all eight micro-sections…" seconds after the harness (`subagent.spawn`) was refused. The distill lane labelled the refusal `ok` because `tool_result_is_error` only looked for a leading `error`/`failed` word, and the hotel's `TaskErrorPayload` text opens with prose (`… \| kind=ipc_failure \| code=SUBAGENT_FORBIDDEN`) | medium | fixed | 2 | 2026-09-14 | codex/bjork-spawn-gate — `tool_result_is_error` recognises the ` \| kind=` payload marker and wholesale `life.observe.batch` rejections; `unretried_failed_tools` = tools whose LAST call failed (a successful retry clears it); `reply_claims_over_failed_steps` (completed-work vocabulary incl. built/spawned/swept/linked/"gone ahead and", no failure acknowledgement) re-enters the model once with a `[Claim check]` hint (`claim_over_failure` event), else delivers with a correction trailer naming the failed tools and drops the memory candidate. Not caught: a reply that never claims completion in the detector's vocabulary |
| DEF-128 | A philote-spawned subagent never received its goal: the philote `subagent.spawn` tool stops at `SpawnSubagentOk`, and only the smoke driver ever sent `AssignSubagentTask`; `deliver_inbound_task` does not park, and the worker registers ~30 ms after spawn returns, so even an eager parent assign would be lost. Live 2026-09-14 20:51 UTC (mac-jane, first spawn after DEF-126): two `philote-worker` guests for `music.repertoire-gardener` registered, accepted their leases, logged "Worker idle — waiting for SubagentDelegation…" and idled past lease expiry; bjork's "I am spawning a fresh subagent" could never be followed by a result | high | fixed | 1 | 2026-09-14 | codex/subagent-auto-assign — `SubagentHookRecord.pending_delegation` holds the hotel-resolved delegation from spawn; `handle_accept_subagent_lease` takes it once and delivers it to the worker inbox (`delegation_task_id` in the accept response); explicit `AssignSubagentTask` unchanged. IPC test: parent spawns → worker registers + accepts → worker inbox receives the `SubagentDelegation` |
| DEF-129 | `philote-worker` cannot execute a skill: `execute_delegation` is a single model call with the goal as prompt ("Multi-turn model loops and tool execution will be wired in subsequent blocks"), `allowed_tools` resolved from the skill catalog are ignored, and the model reply is fired back as the completion summary. Spawn-by-name therefore cannot run any tool-bearing skill (the gardener, the steward, research digests) — the parent gets prose. Live 2026-09-14 21:09 UTC (mac-jane, DEF-128 build): worker `14ce429a` received the gardener delegation 7 ms after lease accept and started executing, then its single model leg (`EmitTask` to role `model`, session `worker-…`) got no reply and failed with `Model response timeout` at 120 s; `Failure hook fired` on the worker side but no hotel-side hook delivery to the parent is logged, and bjork's session shows no follow-up turn | high | open | 8 | 2026-09-14 | needs a seam: give the worker the philote turn loop (tool projection bounded by `allowed_tools`, iteration budget, completion hook carrying the tool ledger) or route skill delegations to a role incarnation instead of a worker; until then a philote that delegates a write to a subagent must be told the result is text-only |
| DEF-130 | Plans stopped one step short whenever the last step was a READ: `verify_plan_steps` looked for a step's distinctive words only in the tool call's *arguments*, so a step like "Inspect registered skills … verify music.repertoire-gardener configuration" bound to `skill.list {}` (or "confirm the batch of observations landed" bound to `life.list {labels:[…]}`) could never verify, no matter how many successful calls ran. `stalls` climbed to 2 → verdict `blocked` → `plan_stopped` + "⚠️ Plan stopped before finishing: 0/1 steps verified" under a reply that had correctly reported what the read found. Live 2026-09-15 13:21 UTC (three `life.list` calls, step 1 "contradicted") and 13:28 UTC (two `skill.list` calls), bjork on mac-jane — the operator's "she always stops before the final step" | high | fixed | 1 | 2026-09-15 | codex/plan-eval-read-steps — Pass B accepts a successful call of the bound read-only tool even when the step text is distinctive (a retrieval has no per-item artifact; still one-to-one, failed reads still no evidence), and a read-only step that follows other steps only takes a call made AFTER the calls that satisfied them (both passes) — the `life.audit` that seeds a gardening plan must not count as its closing re-audit (caught by `audit_result_seeds_a_tidy_plan` on the first cut). Write steps unchanged. 5 tests from the live plan texts |

---

## HealthKit slice fixes (2026-09-04)

IDs allocated after checking `origin/develop` (through DEF-104); this stacked branch predates DEF-081 through DEF-104.

| ID | Title | Severity | Status | Pts | Found | Fixed by |
|---|---|---|---|---|---|---|
| DEF-105 | Unavailable HealthKit generated plausible sample values and uploaded them with real-health provenance; metrics defaulted on and missing data was reported as no metrics enabled | high | fixed | 2 | 2026-09-04 | `codex/ios-healthkit` — real-data-only reader, selected-type consent, local preview and separate confirmation; service regression tests. Seam: device-tool-plane. |
| DEF-106 | Swift observe client normalized `failed`/unknown server statuses to `ok`, and ignored nested batch result acknowledgments | high | fixed | 1 | 2026-09-04 | `codex/ios-healthkit` — fail-closed status mapping, nested result decoding, exact per-observation acknowledgment check; transport/service tests. Seam: lifegraph-read-plane (adjacent write adapter). |
| DEF-107 | macOS hosted app tests initialized real connection settings and blocked in `SecItemCopyMatching` before XCTest started | medium | fixed | 1 | 2026-09-04 | `codex/ios-healthkit` — DEBUG XCTest host renders without a ChatSessionManager; macOS app tests pass without Keychain interaction. Seam: device-tool-plane validation. |

## Open defects — detail

### DEF-001: `local_capability_advertisements_include_hotel_scoped_incarnations` off-by-one

**Found**: 2026-03-10 · **Seam**: active-membrane-routing

`tool-runner` is counted in hotel-scoped incarnation advertisements but is seeded as inactive. The test expects the active set only, producing an off-by-one failure. Pre-existing; does not affect runtime behavior since `tool-runner` is unused. Test still present at `crates/aiua/src/main.rs`.

### DEF-005: `aiua` test suite — 10 hung tests, 2 unrelated failures on develop HEAD

**Found**: 2026-06-22 · **Seam**: aiua-test-infra

`cargo test -p aiua` on unmodified develop (`d2478e8`) hangs indefinitely (0% CPU) on 10 tests: the `desktop_membrane_*` lease/status/target tests, `discord_lease_injects_membrane_binding`, and both `e2e_*_round_trip` tests. Reproduced in a clean detached worktree and single-threaded with one test alone — not parallel contention, not cache corruption. With those skipped, `emit_task_falls_back_to_orchestrator_when_active_incarnation_is_unregistered` and `default_guest_seed_injects_hotel_socket_env` fail order-dependently. Prime suspect for the hangs: tests bind `(dispatcher_tx, _dispatcher_rx)` and never drain the receiver, so any path sending more than channel capacity of `LedgerCommand`s blocks forever on `dispatcher_tx.send(...).await`. Blocks using `cargo test -p aiua` as an automated gate; sweeps work around it via `--skip` filters and live-fire verification.

**Fixed** (`codex/aiua-test-unhang`, 2026-07-03). The dispatcher-channel suspect was real but secondary; four distinct causes stacked:
1. **Client swallowed lease acquire/renew replies (the true hang)** — `PhiloticClient::is_expected_response` expected `DesktopMembraneLeaseStatus` for `AcquireDesktopMembraneLease`, but the server replies `DesktopMembraneLease`, which is also listed in `is_ignorable_push` — so the real reply was skipped as an OOB broadcast and `send_request` read forever. Only the 3 desktop-lease tests truly hung; the other 7 "hung" tests were queued behind them on the shared `ipc_env_guard()` mutex. Fixed by mapping Acquire/Renew requests to their actual reply variants (including the untagged-enum collapse of `DiscordGatewayLease` → `TelegramPollLease`).
2. **Undrained test dispatcher channels** — production's durable-writer thread drains `dispatcher_rx`; test fixtures never did, so chatty tests could block on `dispatcher_tx.send().await` at channel capacity. Fixed with a shared `ipc::test_dispatcher_channel()` helper that drains into an unbounded channel; all fixture sites in `ipc.rs`/`cron_ticker.rs` converted.
3. **e2e tests predated the response-route resolver** — their `model_response` emissions carried no `reply_guest_id`/`return_route` (production model-router always includes both), so the resolver fell back to the session's unregistered `primary_agent_id` and parked the reply. Tests updated to mirror production payloads.
4. **Real routing black hole + stale seed count** — `emit_task_falls_back_to_orchestrator_when_active_incarnation_is_unregistered` exposed that an active incarnation unknown both locally and to the mesh was silently dropped; EmitTask now falls back to the session's live orchestrator when the mesh reroute finds no home (production fix in `ipc.rs`). `default_guest_seed_injects_hotel_socket_env` was stale since PR #94 added the `model.openrouter` guest (11 → 12).

Full suite: 219/219 green in ~21s.

### DEF-024: `brain` role incarnation loops infinitely on mac-jane

**Found**: 2026-06-27 · **Seam**: role-incarnation

After the mesh-auth-key mismatch between mbp-jane and mac-jane was fixed, `agent-astrid:brain` materializes cross-hotel on mac-jane but loops infinitely against Gemini and never replies back to Telegram — likely missing vault/mempalace skills plus an unwired cross-hotel return path. Mitigated by deactivating the guest and reverting Astrid to orchestrator posture; `/role brain` remains unsafe until the loop and return path are fixed. Related but distinct from DEF-030 (the `/role` self-handoff loop, which is fixed).

### DEF-025: Boot-time fallback mesh port persisted as advertised port

**Found**: 2026-07-01 · **Seam**: mesh-transport

`resolve_runtime_ports` (`crates/aiua/src/main.rs:627`) falls back to an alternate port cluster on a boot-time collision and writes the fallback back into the hotel's `graph_nodes` record — while peers keep sending to the originally advertised port. One transient collision permanently diverged mac-jane from its peers (days-long silent mesh-receive blackout; brain TaskInvokes never arrived). Manual fix: reset `hotel:mac-jane` ports + full `launchctl bootout`/`bootstrap`. Peer-side heartbeat reconcile (94edd08) mitigates but the fallback port should not be persisted as the advertised port, or aiua should retry the canonical cluster on later boots.

### DEF-032: `HotelStateSync` broadcast exceeds UDP datagram ceiling (EMSGSIZE)

**Found**: 2026-07-03 · **Fixed**: 2026-07-04 · **Seam**: mesh-transport

**Resolution**: three layers — chunk `model_profiles` across datagrams (PR #104, a5b6f55), measure the FULL signed wire size against macOS's real `net.inet.udp.maxdgram` = 9216 ceiling instead of inner-byte proxies (PRs #107/#109), and a dual-read `BeaconPayload` (legacy int-array OR base64) with the sender flipped to base64 fleet-wide via `PHILOTIC_BEACON_PAYLOAD_B64=1` (~1.33x wire inflation vs ~4x — a realistic 20-guest roster could never fit under 9216 in the legacy encoding). Watched-live-green 2026-07-04: 0 EMSGSIZE on mac-jane post-flip; vps-jane journal shows `Hotel state sync from mac-jane: 30 guests, 3 agents` — the first Mac roster ever to cross the mesh.

Adding the `model_profiles` catalog to the `HotelStateSync` payload (on `codex/model-catalog-sync`) pushed the UDP broadcast over the 65507-byte ceiling — `Message too long (os error 40)` every 30s, dropping guests/agents routing state propagation to peers. Benign for local serving, degrades cross-hotel state sync. Fix `a5b6f55` (chunk oversized broadcasts under the ceiling) is committed on `codex/model-catalog-sync` but not yet PR'd/merged — was queued behind a WAN outage on the dev machine.

### DEF-033: `FailTask` never updates the persisted `session_turn` record

**Found**: 2026-06-22 · **Seam**: session-ledger

`IpcRequest::FailTask` (`crates/aiua/src/service/ipc.rs`) carries only `{error, reason}` with no session/turn envelope, so `extract_session_envelope` bails and the persisted turn stays `running` forever. Record-keeping gap only — in-memory watchdog recovery works and nothing gates new dispatch on the stale status — but it pollutes the DB with false zombie turns (see DEF-022) and misleads diagnostics.

### DEF-034: router-listener startup crash — missing `router_listener.config`

**Found**: 2026-06-04 · **Seam**: guest-lifecycle

The router-listener guest crashes on startup when the `router_listener.config` key is absent from the context graph. Pre-existing regression, observed repeatedly in mbp-jane's heal queue. Guest should seed a default config or degrade gracefully instead of crash-looping.

---

### DEF-059: cross-hotel life.observe lost on runner-hotel restart (at-most-once, false capture confirmation)

**Found**: 2026-07-20 · **Seam**: idea-intake-charter / lifegraph-change-push

Watched-live intake test for the Aria idea pipeline: operator texted Aria an idea; Aria's turn projected life.* correctly and emitted 3 tasks to `vps-jane-aiua-01/life-graph-runner` (02:24:39Z). vps-jane was mid-shutdown (`stop-sigterm` from ~02:23:38, SIGKILL 02:25:08 — parallel deploy). The autorecall (read) completed at 02:24:48; the observe (write) tasks were claimed but never executed and did NOT replay after the hotel restarted — Memgraph shows no node, no error surfaced to Aria, and she confirmed capture to the operator. Beacon (vps) was simultaneously killed mid-reply on its own turn.

Class: mesh EmitTask delivery to datasource runners is at-most-once once claimed; a mid-processing SIGKILL loses the task silently. Same failure class as the Coach `stuck_turn_evicted:WaitingTool` incident (2026-07-14). Fix directions: (a) at-least-once redelivery for datasource tasks (claim → ack-on-complete with redelivery timeout), (b) philote-side write-confirmation in the idea charter (life.recall the node id after observe; retry once, tell the operator honestly on failure), (c) drain window in the vps systemd unit (stop-sigterm grace already 90s — the runner needs to stop claiming on SIGTERM).

### DEF-068: unknown remote EmitTask returned false success

**Found**: 2026-07-27 · **Seam**: cross-host-distributed-validation

A watched-live negative LifeGraph smoke from `mbp-jane` targeted a nonexistent node. The hotel correctly classified `emit_task_unknown_target_node` and created a heal record, but still returned `ok`, appended `TASK_INVOKE`, and left the caller waiting until its 30-second datasource-response timeout. The acceptance boundary now returns `TARGET_NODE_UNREACHABLE` before appending the event. A regression asserts the structured error, classified heal evidence, and absence of a ledger command. This closes false acceptance for a destination that is unknown at dispatch time; it does not close DEF-059's separate post-claim crash/replay gap.

### DEF-073: supervisor race materialized duplicate Telegram gateways

**Found and fixed**: 2026-07-28 · **Seam**: telegram-poll-lease

Jane had two live `membrane-telegram` processes with the same logical guest IDs. A single inbound message generated one Philote `model_response`, but both gateways received the same reply task and each sent a final Telegram response. A heal-triggered gateway restart overlapped the five-second supervisor reconciliation loop: reconciliation retained a stale `active_pid=None` decision while waiting for the materializer lock, refreshed the guest after the replacement was live, but did not re-evaluate the refreshed PID before spawning. Inserting the second child under the same map key discarded the first process handle without terminating it; the poll lease could not distinguish the two because both registered under the same guest ID.

The fix makes restart reclaim-and-respawn atomic under the shared materializer lock, makes reconciliation recheck refreshed PID liveness before spawning, and makes the local process materializer reject a duplicate live child instead of overwriting its handle. Regression tests cover both the stale-snapshot interleaving and the duplicate-child fence.

### DEF-076: the repeat stage of the agent loop still self-certified

**Found and fixed**: 2026-07-29 · **Seam**: philote-agent-loop

DEF-074 grounded the inside of a turn. It left the outside untouched, and the two halves of the cycle then disagreed about what "done" means: `reentry_hint` and `plan_integrity_note` would not let a turn *tell the operator* a plan was finished while a step was unverified, while `evaluate_plan` — the function deciding whether the loop goes round again — read the same plan, took `step.status == "done"` at face value, returned `Complete`, and cleared the carryover. The turn stayed honest and the loop still exited early, which is the same missing artifact by a different route.

The carryover made it self-reinforcing. `steps_done` is settable by a model claim, and it was passed back in as `prior_done` on the next eval, so one unearned `done` became permanent "evidence" for the rest of the plan's life. Evidence is now carried separately as `verified_step_ids`, populated only from `verify_plan_steps` output and keyed by step **id** rather than index — a continuation that splits a bundled step renumbers the tail, and index-aligned flags would credit the wrong step.

Grounding the verdict alone would have made the loop *worse*: settled counts drop once claims stop counting, and the old rule blocked a plan the first time a continuation added nothing. A turn spent on a failed call or a setup read legitimately settles nothing, so a single stall is now tolerated and two consecutive stalls block. That also bounds the case where a step bundles several outcomes: such a step can never settle until it is split, so an unsplit bundle stops on the stall guard after two turns instead of grinding through the whole budget.

Remaining gap: a plan that binds no tools to any step has nothing to check, so its steps still settle on the model's word. That is reported as `basis = "model_reported"` in the `plan_eval` event rather than silently accepted, but it is not prevented.

### DEF-077: the loop could not speak without stopping

**Found and fixed**: 2026-07-29 · **Seam**: philote-agent-loop

Every piece of mid-turn delivery already existed. `partial_replies` is a declared model-router channel; `handle_model_response` extracts it; `emit_partial_reply` dispatches it; membrane-telegram receives `partial_reply` and calls `upsert_formatted_text` against the turn's `draft_message_id`, which the final reply then overwrites. An interim message therefore costs nothing in the transcript — it is edited away when the turn ends.

None of it ran. No `cognitive_response_contract` listed `partial_replies`, so `include_partial` in the model-router was always false and the array was always empty; and the only flush site was the `Respond` arm, which executes as the turn ends. A partial emitted alongside a tool call was discarded.

The consequence was structural rather than cosmetic. Because the sole way to be heard was to finish, a long plan ran silent to its wall-clock budget, the re-entry hint's third exit ("say specifically what you need") had to end the plan even when other steps could still progress, and the hint told the model not to report progress at all. Making `Blocked` more reachable in DEF-076 sharpened this: the loop gained more reasons to stop and still had no voice.

The channel is now requested on the tool re-entry dispatch only — the one call that can be followed by another tool call. Voice is deliberately excluded: an interim on a voice turn would route through the draft path that voice replies delete. Admission is a pure function of the turn clock (`interim_reply_admissible`), stamped inside `emit_partial_reply` so the plan-progress, handoff, and paracrine paths all draw on one quiet period.

Not addressed here: a plan that stops via `PlanFollowup::Stop` still reports only as a `plan_stopped` turn event. `active_turn` is already `None` by then (`complete_active_turn` runs before the follow-up dispatches), so the interim channel cannot carry it; making that audible needs a membrane-telegram change and re-opens the DEF-074 decision about machinery narration in chat.

## Fixed defects — detail

### DEF-002: Abstract tool storage methods unwired (legacy `ansible` crate Gap 3)

`upsert_abstract_tool` / `get_abstract_tool` / `list_abstract_tools` were defined but not wired in the old `ansible` crate's sqlite storage, breaking its build. Resolved by the port: the methods are implemented in `crates/ansible-mesh-core/src/domain/mod.rs` and the workspace compiles.

### DEF-003: `aiua` binary test target could not compile/run

Missing `OperatorTargetView` import, incomplete `WorkflowSkillRecord` mock impls, and a missing `hotels["default"]` fallback in `extract_context_graph_entries` prevented `cargo test -p aiua` from running at all. All three fixed; 110 tests passed at close. (The later, separate hang is DEF-005.)

### DEF-004: Multi-tool synthesized response not surfacing

Re-entry loops completed tool iterations but the final synthesized summary never reached the user. No isolated fix commit; the cognitive loop was rebuilt in PR #58 (cognitive loop v2) and PR #59 (resilient loop, all 6 slices) and the failure has not reproduced since 2026-04. Closed as superseded.

### DEF-006: Watchdog evicted WaitingModel turns instead of escalating

When a model call hung past `WAITING_MODEL_SECS = 300`, the philote watchdog evicted the turn ("I seem to have gotten stuck") instead of escalating to the next fallback tier. Underlying trigger: silent IPC drop between model-router and philote leaves no error signal, so the watchdog is the only backstop. An earlier "fix" (951e113, 120s→300s bump) only delayed the give-up. PR #93 (2e33928) branches on `WaitingModel` in `evict_timed_out_turns` and calls `advance_turn_to_next_fallback_tier`; the 600s CatchAll ceiling remains a hard evict. Deployed to mac-jane 2026-06-29.

### DEF-007: Voice routing lost on checkpoint restore

Checkpoint snapshots didn't serialize `agent_profile`, so restored sessions got a default `VoiceResponsePolicy` (provider=None) and always fell back to ElevenLabs, ignoring the agent bundle's `"onnx"` setting. Fixed in `ensure_session_loaded` by applying `default_agent_profile` after `from_checkpoint`, mirroring the new-session path (shipped with PR #60).

### DEF-008: `IpcResponse` untagged-enum ordering swallowed `MemoryConfig`

`UserProfileData { Option, Option }` sat before `MemoryConfig { Option }` in the `#[serde(untagged)]` enum; an all-optional variant matches any JSON object, so `{ "config_json": ... }` deserialized as an empty `UserProfileData` and philote ran without memory. Fixed by extracting a `UserProfileDataPayload` wrapper with `#[serde(deny_unknown_fields)]`. Standing rule: every new all-optional variant needs a `deny_unknown_fields` wrapper; `MemoryConfig` stays last.

### DEF-009 / DEF-010: Gemini streaming hangs (batch fallback; keep-alive drip)

Two related hangs. (1) Gemini streaming sometimes returns SSE with no text (safety block, quota); the no-content path fell back to the batch endpoint which had no timeout — observed 27-minute hang. PR #59 removed the batch fallback (bail with `streaming_timeout`, routed to tier escalation) and added an 8s per-chunk idle timeout. (2) Gemini can drip keep-alive SSE bytes every ~7s, resetting the idle timer forever without progress; f7f8715 added a hard wall-clock cap on the whole SSE session (`STREAMING_TOTAL_SECS`). 816dadd later added connect/send timeouts in the streaming forwarder to close a related deadlock. Do not re-add the batch fallback; the correct escape is tier escalation.

### DEF-011: membrane-mcp orphan storm

Stale membrane-mcp processes accumulated and fought over a single lease (~5 lease attempts/sec observed with 7 orphans). 8abef40 made ghost reclamation kill stale PIDs directly; 18cbf14 eliminated the remaining accumulation path in guest-manager.

### DEF-012: Local-agent tools with no execution route

`hotel.status`, `hotel.logs`, `role.set_home` were in the abstract tool catalog and toolset profiles but missing from `is_local_agent_tool()` in `crates/aiua/src/service/ipc.rs`, so they silently had no route. Rule: any tool handled in philote's `execute_local_agent_tool` match MUST be added to `is_local_agent_tool`.

### DEF-013: Malformed `session_turn` record killed snapshot composition

An old binary wrote `completed_at` as a JSON string; `list_session_turns` used `?` per record, so one bad row failed the entire `compose_session_snapshot`, silently blocking toolset injection for the whole session. Hardened to warn+skip in `ansible-mesh-core/src/domain/mod.rs`; bad rows repaired via SQL.

### DEF-014: OOB broadcasts broke request/response on the guest socket

Hotel-initiated OOB broadcasts (`MuninnStatus`, `NetworkState`) arriving mid-`send_request` were consumed as the response, wedging membrane-telegram seats. 63f7072 buffers OOB push messages in `philotic-client::send_request` instead of misinterpreting them.

### DEF-015: Paracrine reply drops and unrouted delegate.merge

`reply_to_guest_id` for "self" whispers was set to the membrane seat, so specialist (brain) replies went brain → membrane → silently dropped instead of returning to the orchestrator (d0b969e). Separately, `delegate.merge` had no execution route in paracrine specialist turns (542d967). Confirmed e2e: Astrid → delegate.whisper → brain → synthesized Telegram reply.

### DEF-016: `mesh_events` unbounded growth

The append-only event ledger never pruned; 84,904 stale events from a renamed hotel had to be deleted by hand before a2ed50d added retention.

### DEF-017: Cron-fired tasks silently dropped for dormant role incarnations

Beacon's daily Chronos heartbeat failed silently for days: `target_role` was stored bare (`"orchestrator"`) instead of the routing key (`role:agent-beacon:orchestrator`), and even a correct key found zero subscribers when the role guest was dormant. PR #80 added `normalize_cron_target_role` at registration and park-and-materialize via `ensure_role_materialized` in `CronTicker::fire` (the first attempt wrongly reused the cross-hotel helper — caught in review). Live-verified on vps-jane: park → spawn → register → flush → delivery.

### DEF-018: `UserProfileData` treated as ignorable push

`UserProfileData` in `is_ignorable_push` caused a hang in the client push path. PR #81 removed it and added a guard test against re-adding it.

### DEF-019: Outbound responses parked ledger-only for non-live incarnations

Inbound delivery checked whether `session.active_incarnation_id` was a live registered guest and fell back to the orchestrator; outbound (`infer_response_target_guest_id_for_agent_task`) did not — replies resolved to an unregistered guest, logged "stays ledger-only for now", and were swallowed until the 300s watchdog fired. PR #84 mirrored the liveness fallback; live-firing showed PR #84 alone was insufficient because the fallback guest tripped `targets_base_agent` re-derivation in `resolve_agent_route`, which PR #85 made response-like payloads skip. Bit every hotel independently (vps-jane Beacon, then mac-jane Bjork within minutes); all three patched 2026-06-22.

### DEF-020: LifeGraph cross-hotel calls parked forever

Every `life.*`/`graph.query` call from mac-jane/mbp-jane routes cross-hotel to `vps-jane:life-graph-runner` — a hotel-level utility guest with no `role_incarnation` record. The cross-hotel dispatch path assumed the target was a dormant role guest, logged "No role incarnation found for cross-hotel guest; cannot materialize", and parked the task forever; the 90s WaitingTool watchdog then evicted the turn. 100% reproducible. PR #86 guards `deliver_event_envelope_or_park` to skip events addressed to another node. Live-verified: cross-hotel round trip in ~1.46s, no park.

### DEF-021: Conversational-goal gate zeroed all tools on "look"

`looks_like_conversational_goal` used plain `str::contains` against a filler list including `"ok"` — a substring of "look"/"took"/"book" — so natural requests like "take a look at the lifegraph" tripped the conversational heuristic and `project_tools_for_turn` returned an empty tool list for the whole turn with no fallback. PR #87 added word-boundary matching. Verification lesson: the standard smoke driver names the tool explicitly and bypasses this gate — regression checks must use natural phrasing.

### DEF-022: Zombie running turns after restart storms

Stuck `status=running` turns accumulated across hotel restarts (64 found on bjork, oldest from May), partly downstream of DEF-033. PR #91 added a startup sweep plus a heal-dispatcher scan that proactively repairs them.

### DEF-023: Role handoff arrived with no context and never executed

`handle_handoff_bundle` read only `bundle.working_summary` — always `None` from `handoff.to_role` — so receiving roles had zero context; and nothing triggered execution, so the role sat idle until another operator message. PR #92 synthesizes the summary from `goal + context_excerpt` and pushes a synthetic `role_directed_task` to `pending_drains` when `active_goal` is set.

### DEF-026: Provider secrets in Muninn config

The hotel's Muninn config payload included provider secrets from the vault registry. PR #95 filters them out.

### DEF-027: Voice synthesis and Telegram streaming broken

A keys/streaming regression broke ElevenLabs synthesis and Telegram streamed replies. PR #98 (c5e6263) restored both.

### DEF-028: Telegram poll lease lost on network flap

Marginal WiFi (reachability probe TCP-connects 1.1.1.1:53 with a 3s timeout) flapped the seat offline; missed renewals let the lease TTL lapse, and the client treated "lease lost to None" as contested — entering a never-resetting backoff that left seats dark up to 600s per flap. PR #99 (648acf9) re-acquires the lease in place.

### DEF-029: Approval buttons never resolved parked turns

philote's approval resolver only acted on `/approve`/`/deny` text, but membrane turned inline-button callbacks into plain "Telegram callback action: X" chat — so approval-parked turns always hit the 300s watchdog (7 occurrences since May 29). PR #99 (1b86a45) maps approve→`/approve`, deny→`/deny`, trust→`/approve`.

### DEF-030: `/role` self-handoff infinite loop

Issuing `/role` for the role a session was already in triggered an infinite role-swap loop. PR #101 (1b90cfa) hardened the handoff path with a loop guard + self-heal; PR #102 (6f992f7) makes `/role` to the current role a no-op.

### DEF-031: TTS replies rendered as music cards

Telegram TTS replies were sent as audio files (music cards) instead of voice notes. PR #102 (36e73c2) switches delivery to `sendVoice`.

---

## Technical debt

One line each; severity + pointer. These are structural, not behavioral defects.

- **Two drifting Telegram gateways** — medium — `crates/membrane` (legacy gateway) and `crates/membrane-telegram` implement overlapping Telegram logic and drift apart; fixes like DEF-028/029 land in one and not the other.
- **Graph full_scan is destructive, not incremental** — low — every scan (now scheduled 6h) clears and re-upserts all nodes; `body_hash` is stored but unused for skip. Embedding wipe (the harmful effect) fixed by DEF-066 snapshot/restore; remaining cost is scan time + id-churn losing ~3 embeddings/scan. Revisit when the graph outgrows seconds-per-scan (11k nodes today).
- **Four hand-rolled lease clients** — medium — membrane-telegram, membrane-discord, membrane-mcp, and the desktop membrane each reimplement lease acquire/renew/backoff (`lease.rs` per crate); DEF-028's bug class exists ×4.
- **park_and_materialize duplication + cron dual-delivery race** — medium — `park_and_materialize_local_role` vs `park_and_materialize_role_philote` in `crates/aiua/src/service/ipc.rs`, and a fired cron event is consumed by BOTH `CronTicker::fire` delivery and `deliver_event_envelope_or_park` reacting to the same `AppendLocal` — which one wins varies per fire (observed live, session 18).
- **Guest supervisor off by default** — RESOLVED (codex/supervisor-default-on) — supervisor loop now runs by default with flap protection (max 5 respawns per guest per 10-min window; breach marks `supervision_state:respawn_budget_exhausted` in the graph and pushes a heal-queue entry). Opt out with `PHILOTIC_DISABLE_GUEST_SUPERVISOR=1`; legacy `PHILOTIC_ENABLE_GUEST_SUPERVISOR` is deprecated (truthy = no-op, falsy = opt-out). Awaiting 24h watched-live soak on next fleet deploy.
- **Unbounded paracrine chains** — medium — the cross-hop budget (`charge_paracrine_hop`, default 5 hops / 900s) is charged ONLY in the `EnrichedToolResult` routing branch; `ReflectiveReEntry`/`CognitiveReEntry`/`MemoryEnrichment`/`DatasourceInjection`/`PriorityReEntry` all re-enter via `handle_user_message` on a fresh turn with `paracrine_hop_count` reset, so async whisper→response→whisper chains through those variants have no cross-turn bound (each turn is still iteration-capped + watchdog-bounded). Partially mitigated by the reflective-surface fix (paracrine PR — a reflection reply now surfaces to the user instead of silently ping-ponging, so a runaway loop produces visible output a human can stop). A proper per-chain (session-scoped) budget needs live validation and is deliberately deferred rather than added blind to the hot path.
- **Role-incarnation model reply misrouted to base agent** — RESOLVED (paracrine-model-route) — the real reason whispers to a role specialist (e.g. `theoretician`) always hung: a role-incarnation philote runs as a SEPARATE process (`{agent_id}:{role_name}`) but built its `ModelRequestPayload` with only the bare base `agent_id`, so `ReturnRoute::from_task`'s `agent_id` fallback addressed the model RESPONSE to the base agent's process — the specialist's turn never received its reply and rode the watchdog to a zombie reap (`RepairStaleSessionTurns`, 300s). Confirmed live mac-jane 2026-07-15 17:21 (45 model deltas all to `agent-bjork-01`, zero to `agent-bjork-01:theoretician`) and identically 2026-07-10. Fix: philote sets `ModelRequestPayload.reply_guest_id` to its own incarnation id (`AgentRuntime::model_reply_guest_id`), which beats the `agent_id` fallback so the reply returns to the incarnation process (which subscribes to role `agent` under that guest id). NOT a gemini stall and NOT churn.
- **On-demand role-philote churn** — low — a whispered role that isn't already materialized is spawned on demand and the supervisor respawns it periodically; separate from the model-reply misroute above (now fixed). Every whisper to a cold role still pays a materialization + first-model-call latency penalty; keeping hot roles warm is a future optimization, not a correctness bug.
- **Primitives crates unused** — RESOLVED (2026-07-06, PR #147 `codex/crate-cleanup`) — the five empty `philotic-primitives-{agent,data,hotel,model,tool}` scaffolds (zero reverse dependencies, never workspace members) were deleted along with the retired `graph-runner` crate; `philotic-primitives-mesh` is the only surviving primitives crate and stays (consumed by `ansible-mesh-core`).
- **agent-graph-runner dead directory** — low — `crates/agent-graph-runner` has no `Cargo.toml` (not a buildable package, not a workspace member) but the directory and its `src/main.rs` still live on disk, superseded by `agent-datasource`; deletion pending. Historically caused orphaned-PID cleanup work from stale built binaries.
- **membrane-discord unshipped island** — low — `crates/membrane-discord` is a full gateway (voice bridge included) that is never seeded or deployed; it still costs build time and carries its own lease client.
- **membrane-mcp dead dispatch.rs** — low — `crates/membrane-mcp/src/dispatch.rs` is declared (`mod dispatch;`) but nothing references it; dead code to delete or wire in (folded into `mcp-membrane-hardening` slice H5).
- **membrane-mcp dispatch surface untested** — high — zero tests cover `server.rs`/`routing.rs`/`main.rs` (the whole HTTP JSON-RPC ingress: fence tiers, auth × approval matrix, endpoint-vs-legacy mode selection, oneshot correlation); only `auth.rs`/`transform.rs` have unit tests and the sole e2e is the bash UAT script (`mcp-membrane-hardening` slice H6/H5).
- **membrane-mcp dual dispatch modes half-merged** — medium — legacy per-agent route-table mode (`UpdateMcpRoutes`) and config-driven endpoint mode coexist; the default `{hotel}:membrane-mcp` guest (`MCP_MEMBRANE_REQUIRED`) only ever runs legacy mode and never receives endpoint configs, while per-endpoint guests never serve legacy routes (`membrane-mcp/src/main.rs:204`, `aiua/src/service/ipc.rs:7924`); retirement plan in `mcp-membrane-hardening` slice H2.
- **MCP IPC identity gap** — medium — `ProvisionMcpEndpoint` trusts a self-asserted `owner_agent_id` and the generic `SetConfig` IPC writes any key (including `__mcp_endpoint__:*`/`__mcp_routes__`) with no ACL; only `RevokeMcpEndpoint` checks ownership (`aiua/src/service/ipc.rs:3995-4001,7829-8077`); fix = identity-at-IPC + reserved `__mcp_` prefix, `mcp-membrane-hardening` slice S4.
- **Mesh-key / OIDC decrypt mismatch on macOS hotels** — medium — operator-flagged 2026-07 (mac-jane / mbp-jane): the underlying keychain-vs-file root-key nondeterminism is fixed by DEF-038 (PR #170, deterministic env -> file -> keychain order), but OIDC provider-secret decrypt failures in the desktop auth path have not been independently re-verified live under the new deterministic order — treat as open until confirmed on a fleet deploy.
- **mesh-config.json plaintext provider keys** — medium — `mesh-config.json` / `mesh-config.example.json` still carry provider API keys in plaintext for local bootstrap, ahead of the vault-backed `provider_keys`/`*_secret_ref` path that hotels use once running; hygiene gap between first-boot config and steady-state vault storage, no tracked remediation slice yet.
- **TTS audio inlined into session_turn records** — medium — kokoro/voice turns embed the full WAV as `audio_base64` inside `response_json`, producing multi-MB `session_turn` nodes (8.6MB observed) in the context graph; the :9001 blob store exists for exactly this. Retention (DEF-080) now caps the damage at 7 days, but audio artifacts should be blob refs, not graph payloads. No tracked slice yet.
- **`collect_agent_vault_names` still broken for Dreams + M3 digest** — medium — DEF-066 fixed vault discovery for the `memory.hygiene` sweep only (`memory_hygiene.rs`, `codex/hygiene-vault-discovery`); `dream.rs`'s DreamsPhase consolidation (`dream.rs:135`) and the M3 memory-delta digest (`memory_delta_digest.rs:453`) still call the same `dream::collect_agent_vault_names`, which matches zero guests on every real hotel (same top-level-vs-`env.PHILOTIC_AGENT_ID` mismatch as DEF-066). Both are silently no-oping the same way the hygiene sweep was. Fix = point both at `memory_hygiene::discover_vaults`/`resolve_vaults_to_sweep` (or a shared helper) instead of the guest-derived scheme; not fixed here to keep this slice to its named seam.
